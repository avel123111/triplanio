---
name: triplanio-timeline-account-edit-batch
description: "Батч UI 2026-06-12 — таймлайн day-separator/today/rail, EventAiBlock доводка (badge/btn/subtitle/error), Account inbox-карточка, TripStructureEdit start/finish даты+высота"
metadata: 
  node_type: memory
  type: project
  originSessionId: 15234ac1-581c-4207-915f-1e473a663ef4
---

★2026-06-12 (triplanio_new) батч правок поверх редизайна AI-блока:

**EventAiBlock доводка (A):** Pro-плашка теперь дизайн-системный `<Badge variant="pro" icon="pro">` (мёртвый `.pro-pill` выпилен из app.css). Кнопка распознавания = канон `event.ai_recognize_booking` («Распознать бронь») вместо `ai_recognize`. Сабтайтл шапки унифицирован: collapsed(available) и open(idle) оба = `event.ai_available_hint` (удалён дубль `ai_paste_hint`; `ai_recognize` тоже удалён — оба из en/es/ru). Ошибка распознавания: вместо сырого «Edge Function returned a non-2xx status code» ловим FunctionsHttpError/`non-2xx` в catch `runParse` и показываем дружелюбный `event.ai_parse_error` (обновлён во всех локалях: «Не удалось распознать документ. Попробуйте снова или выберите другой файл»). Файл: `EventAiBlock.jsx` + локали event.js.

**Таймлайн day-separator (B) — `TripView.jsx` `TimelineLens`:** шапка дня переверстана: `[datechip]` слева + колонка `.tl3-dhx` (строка `.tl3-dhrow` = погода `.wthr` остаётся у дня недели; под ней строка городов `.daycity` — лёгкая, иконка-пин + имена, без тяжёлой `surface-2`-пилюли). Город(а) дня = ВСЕ реальные transit-города, покрывающие день, в порядке, через « · » (мульти-сити «Мадрид · Севилья»); **waypoint (kind==='waypoint') исключены** из списка. `data-city` для observer = последний реальный город дня. Сегодня: класс `.tl3-day.today` → datechip залит `--brand` (текст `hsl(--primary-foreground)`) + бейдж `.tl3-today` («Сегодня», `view.today`). Автоскролл к сегодня при открытии таймлайна (useEffect+ref-гард, `scrollIntoView`, no-op если today вне диапазона). CSS в app.css (`.tl3-dh/.tl3-dhx/.tl3-dhrow/.daycity/.tl3-today/.tl3-day.today`).

**Правый рейл (B) — `CityRail`:** фильтр городов теперь исключает waypoint (`v.kind !== 'start' && !== 'end' && !== 'waypoint'`) — только transit-города как «станции».

**Account inbox-карточка (C) — `ScreenAccount.jsx`:** новая `.acct-inbox` карточка первым блоком в `.acct-content` → редирект `/inbox`, бейдж непрочитанных. Счётчик из `useQuery(['notifications', user?.email])` (тот же ключ, что у `NotificationsBell` → общий кэш, без лишнего фетча; RLS-скоуп). Ключи `account.inbox_title/sub/unread` (en/es/ru). CSS `.acct-inbox*` в app.css.

**TripStructureEdit start/finish (D) — `GridEndpoint`:** анкеры стали ниже (`.te-end` padding 15px→9px, ниже transit `.te-row`). Даты: проп `date` — старт = `seq[0].start_date` (старт трипа), финиш = `seq[last].end_date`, **+1 день если трансфер в финиш `day_change`** (овернайт; `arrivalFor(endNode.id)`, mirror server recompute_trip). Раньше показывало «Salida · -» (анкеры без дат).

★Доводка 2 (та же сессия, фикс ревью Pavel): (1) `.btn--ai` в коде имел `--fg:#0c0a1e` (тёмный) в БАЗЕ — баг: текст AI-кнопки был чёрным в light. Исправлено по дизайн-системе LUMO: база `--fg:#fff` (light=белый) + `[data-theme="dark"] .btn--ai{--fg:#0c0a1e}` (dark=тёмный, т.к. в dark ai-gradient ярче). Глобально для всех `.btn--ai`. (2) Pro-бейдж в EventAiBlock: убрал `icon="pro"` (корона ломалась на 2 строки в тесной шапке) → `<Badge variant="pro" style={{fontSize:'var(--fs-micro)',padding:'2px 8px',flexShrink:0}}>Pro</Badge>` — text-only золотой пилл по A4. (3) Плашки событий таймлайна (`.tl3-card`): добавлен `flex-wrap:wrap`, `.body` теперь `flex:1 1 auto;min-width:130px` (было `min-width:0` → схлопывалось, текст ломался по буквам), убран `word-break:break-word` с `.body b/.sb`, `.meta margin-left:auto→0` (цена+пилл переносятся вниз слева под заголовок). (4) Account inbox-карточка ПЕРЕНЕСЕНА из топа `.acct-content` (была над hero/заголовком — плохо) ВНУТРЬ секции `#acct-notify` после `<h2>`.

★Доводка 3 (фикс ревью Pavel): (1) Pro-бейдж в EventAiBlock — ОКОНЧАТЕЛЬНО = общий дизайн-системный `<Badge variant="pro" icon="pro" style={{flexShrink:0}}>Pro</Badge>` (золотой градиент+корона, ИДЕНТИЧНО SettingsLens:77 / Trips). `flexShrink:0` держит корону+Pro в одну строку в тесной шапке. Свой text-only «поделочный» бейдж удалён. (2) Гонка доступности AI-блока: `aiState` init был `'available'`, а `isPro` резолвится асинхронно (`checkSubscriptionStatus`) — не-Pro мог открыть и юзать парсер до проверки. Фикс: init `'checking'` + сброс в `'checking'` в hydrate-эффекте на каждом open; новое состояние `checking` в EventAiBlock = неинтерактивная плашка со спиннером (не кликается). Sync-эффект ставит available/locked когда isPro резолвится. (3) Account: секция/нав `account.email_notifs` переименована «E-mail уведомления»→«Уведомления» (en Notifications/es Notificaciones); чтобы не дублировать с карточкой, `account.inbox_title` карточки → «Входящие»/Inbox/Bandeja de entrada. (4) CityPanel (панель города в trip edit): футер выровнен под event-панели — delete `<Btn variant="danger" size="sm" icon="trash">{common.delete}</Btn>` (была ghost-trash «стремная»), done тоже `size="sm"`.

★★ROOT CAUSE Pro-бейджа (доводка 4, наконец): бейдж-компонент был ВЕРНЫЙ всё время — проблема в CSS-соседе. Правило подзаголовка `.ai-blk-ti span{font-size:11px;color:var(--muted);font-weight:600;display:block}` перекрывало `.badge--pro` (специфичность `.ai-blk-ti span` 0,1,1 > `.badge--pro` 0,1,0), потому что Pro-`<Badge>` — это тоже `<span>` внутри `.ai-blk-ti`. → бейдж рендерился 11px СЕРЫМ 600 вместо 12.5px золотого 800. Это и есть «серый текст» из прошлых ревью. ФИКС: `.ai-blk-ti span` → `.ai-blk-ti > span` (только прямой потомок = подзаголовок; не течёт на бейдж и на `.ai-spin`). Объективно измерено в браузере: после фикса in-block бейдж = standalone (12.5px / #4A2D05 / 800 / 64×25). Урок: при «компонент правильный, а выглядит не так» — проверять специфичность родительских селекторов (особенно `.parent span/div`), а не переписывать компонент.

Проверено: build+eslint зелёные, check:design без новых нарушений (8 старых в CalendarLens.css), скриншоты light/dark (доводки 2,3,4) + light/dark/mobile (батч); размеры бейджа сверены через getComputedStyle. Связано: [[triplanio-ai-pattern-eventaiblock]] [[triplanio-overview-screen]] [[triplanio-editor-ui-redesign]] [[triplanio-overnight-transfer-flag]] [[triplanio-sandbox-redesign]]
