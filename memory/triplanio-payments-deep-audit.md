---
name: triplanio-payments-deep-audit
description: Глубокий аудит платежей 2026-06-21 — заключение + P0 обхода оплаты через клиент-записываемые колонки энтайтлмента
metadata: 
  node_type: memory
  type: project
  originSessionId: aadf1cd0-27a2-4b03-a155-90b57372fa0b
---

★АУДИТ 2026-06-21 (отчёт PAYMENTS_DEEP_AUDIT_2026-06-21.md в «Triplanio docs»; код НЕ менялся, БД не трогалась, эксплойты не запускались).

**Вердикт:** ядро (ledger trip_subscriptions → single-writer recompute_user_entitlement → кэш users.subscription_*/trips.is_pro_trip; идемпотентный webhook; reconcile-on-read; задел под RevenueCat) — солидное, переписывать НЕ надо. Оси: стабильность 7, масштабируемость 8, логичность 8, лёгкость/прозрачность 6, best-practices 6, payments-безопасность обход-оплаты 3, непредоставление-оплаченного 8.

**🔴 P0 (главное):** роль `authenticated` имеет колоночный UPDATE на `users.subscription_status`, `users.subscription_end_date`, `users.stripe_customer_id`, `trips.is_pro_trip`; RLS users_update_own/trips_update без WITH CHECK, защитного триггера/rule НЕТ — **проверено prod И dev**. Любой залогиненный юзер PATCH'ем через PostgREST выдаёт себе Pro бесплатно (и навсегда: reconcile-on-read не сработает при status='pro'+future date+нет customer_id). Владелец так же ставит is_pro_trip=true (проходит даже серверный гейт updateTripSettings). Запись чужого stripe_customer_id → хайджек чужого billing-portal + Pro за чужой счёт. Фикс: REVOKE INSERT,UPDATE этих колонок у anon/authenticated, писать только service-role.

**✅ P0-1 ИСПРАВЛЕНО (T1, 2026-06-21, prod+dev):** энтайтлмент-колонки теперь только-сервер. Миграция 0054: `REVOKE INSERT,UPDATE ON users/trips FROM anon,authenticated` + `GRANT INSERT(cols),UPDATE(cols) ON ... TO authenticated` (только не-энтайтлмент колонки); anon записи не возвращали (RLS требует auth.uid()→anon=NULL, писать незачем). Верифицировано has_column_privilege в обоих проектах: табличных INSERT/UPDATE=0, subscription_status/subscription_end_date/stripe_customer_id/entitlement_synced_at/is_pro_trip закрыты по INSERT И UPDATE, легитимные колонки (full_name/title и т.д.) пишутся. **ДВЕ ГРАБЛИ Postgres (стоили несколько итераций):** (1) колоночный REVOKE — no-op против дефолтного табличного GRANT ALL Supabase: надо снять табличный, потом грантить колонки; (2) `GRANT INSERT, UPDATE (cols)` — список колонок привязан ТОЛЬКО к последней привилегии (UPDATE), а INSERT выдаётся на уровне ТАБЛИЦЫ → дыра остаётся; писать `GRANT INSERT (cols), UPDATE (cols)`. **Грабли на будущее:** новые колонки users/trips НЕ будут писаться authenticated, пока не грантнуть явно. Git: коммит 6598f06 на dev, НЕ запушен (песочница без GitHub-кред + stale .git/HEAD.lock) — ждёт push dev + cherry-pick в main с машины Pavel. Миграция уже применена в обе БД вручную. Аудит данных: prod 3 users status='pro' без stripe_customer_id (вероятно тестовые) + 1 trip is_pro_trip — на ручную сверку с ledger (не чистил). Остаётся T2..T7.

**🟠 P1:** parseBookingWithAi без Pro/membership-гейта (любой free жжёт платный AI-парсер); рефанд/диспьют ПОДПИСКИ не снимает Pro (матч по stripe_payment_intent_id=null у recurring, снимается только pro_trip); набор событий Stripe-endpoint не верифицирован (MCP не дал webhook-API — сверить Dashboard live+test на 8 событий).
**🟡 P2:** обход бюджет-аддона через прямой REST (trip_budgets/budget_expenses RLS = is_trip_participant, не гейтит аддон); дубль источников истины (pro-addon set ×3, формула Pro ×5, лимит ×3); pro_trip-ретрай после краха → петля+ложный double_paid; нет advisory-lock.
**РЕШЕНИЯ/NEXT (2026-06-21):** ТЗ фиксов = `PAYMENTS_FIX_TZ_2026-06-21.md` (T1..T7). RevenueCat ОТЛОЖЕН (ничего не делаем), нормализуем Stripe, но ядро энтайтлмента держим провайдер-агностичным (ledger уже имеет provider/platform/provider_meta). **Старт = T1** отдельной миграцией колоночного REVOKE INSERT,UPDATE у anon/authenticated на users.{subscription_status,subscription_end_date,stripe_customer_id,entitlement_synced_at}+trips.is_pro_trip (для T1 решений от Pavel не нужно; процедура план→апрув→код→git dev+main→ручной деплой prod+dev→смоук). ВАЖНО уточнённый факт: фронт пишет в users {full_name,avatar_url,language,notify_email_invites,notify_email_updates}+клиентский INSERT строки (id,email,full_name,avatar_url) в AuthContext (НЕ триггер), в trips — cover_image_url/cover_gradient (ManualPlanner); энтайтлмент-колонок не пишет → REVOKE строго колоночный (табличный сломает имя/обложку); subscription_status DEFAULT 'free' подтверждён.

**✅ T6-ядро + T2 СДЕЛАНО+ЗАДЕПЛОЕНО 2026-06-21 (коммит bdd9eaa на dev поверх T1 6598f06, оба НЕ запушены):** Миграция 0055 — SQL `is_user_pro(uid)`/`is_trip_pro(trip_id)` (SECURITY DEFINER, stable, search_path=public; семантика как create_trip: null end_date=НЕ pro; execute ТОЛЬКО service_role — пришлось явно `revoke execute from anon,authenticated`, т.к. Supabase их дефолтно грантит, `revoke from public` не снимает; 0045-функции тоже проверены — закрыты). Применена prod+dev, проверена на данных+advisor. `parseBookingWithAi` гейтнут: требует trip_id + isCallerParticipant + rpc is_trip_pro → 403 FORBIDDEN/PRO_REQUIRED до n8n; FE (EventEditDialog→EventAiBlock→invoke) шлёт trip_id; задеплоено prod v12/dev v14 verify_jwt=true; lint+60 тестов зелёные (build не собрать в песочнице — EPERM на dist, не код). ОСТАЛОСЬ T6: перевести 5 инлайн-копий (getActiveTrips/copyTrip/updateTripSettings/getUserPlan/checkSubscriptionStatus + create_trip) на хелпер. **P2-5 бюджет — ОТМЕНЁН (Pavel): системные траты events/services пишутся всегда, RLS не трогать.** План: PAYMENTS_REMEDIATION_PLAN_2026-06-21.md. Stripe: default_price у всех 3 продуктов проставлен; changeSubscriptionPlan удалён из прода; 8 событий вебхука подтверждены Pavel. T1-факт: P0 закрыт live в ОБОИХ (только SELECT+REFERENCES). Также сделано: миграция 0056 — create_trip переведён на is_user_pro (поведение 1:1, применено prod+dev, коммит 1cde163 НЕ запушен).

**★T3 НАПИСАН но НЕ закоммичен/НЕ задеплоен (git-лок песочницы EPERM):** stripe-webhook ветка charge.refunded/charge.dispute.created — подписка снимает Pro (charge→invoice→subscription по stripe_subscription_id, т.к. у recurring PI=null); dispute снимает всегда, refund только полный (amount_refunded≥amount), частичный нет; .error на SELECT проверяется (throw→retry); неатрибутированный возврат→Sentry. Файл изменён+застейджен, lint+60тестов зелёные. **Pavel: rm .git/index.lock → commit T3 → push dev → deploy stripe-webhook --no-verify-jwt в ОБА (canon-10!) → смоук.** Прод-FE(main) отстаёт — parseBookingWithAi на проде уже требует trip_id, накатить main до трафика. **Полный handoff след. сессии: `Triplanio docs/PAYMENTS_HANDOFF_NEXT_2026-06-21.md`** (+ план PAYMENTS_REMEDIATION_PLAN_2026-06-21.md). Осталось: T4(один обработчик возврата)→T5(Sentry)→T7(вебхук-устойчивость)→в конце слить 5 edge-копий предиката+тест-замок. Грабли: Supabase дефолтно грантит EXECUTE anon/authenticated новым функциям (нужен явный revoke); песочница не даёт rm .git/*.lock и dist/* (EPERM).

**Заметки:** баг cancelled в getUserPlan УЖЕ исправлен (читает cancel_at_period_end); Ф5/TRIP-82 expire+recreate в коде ЕСТЬ — доки (7 шт за день) отстают от рантайма. Связано с [[triplanio-payments-phase-status]], [[triplanio-viewer-write-rls-escalation]], [[triplanio-pro-model]].
