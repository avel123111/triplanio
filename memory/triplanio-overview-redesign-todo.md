---
name: triplanio-overview-redesign-todo
description: ★МАКЕТ v2 2026-08-24 — редизайн экрана Overview, выбрана раскладка «Сцена» + виджет «Подготовка» (чек-лист броней отели+переезды); в код НЕ перенесено
metadata: 
  node_type: memory
  type: project
  originSessionId: 829b402b-1f17-413f-b3c3-dcdc0e672ac4
---

★МАКЕТ v2 2026-08-24, 2 итерации (в код НЕ перенесено): интерактивный прототип редизайна экрана **Обзор** ([[triplanio-overview-screen]]) с новым виджетом **«Подготовка»**. Заменяет макет 2026-06-16 (To-Do «недостающие брони») — концепция виджета пере-утверждена Pavel: не «только дыры», а ПОЛНЫЙ счётный чек-лист.

**Виджет «Подготовка» (контракт):**
- Слоты: отель на КАЖДЫЙ город (city_visit кроме транзита) + переезд на КАЖДЫЙ стык соседних городов (N−1). «Забронировано» = сущность СУЩЕСТВУЕТ (hotel_stay для города / transfer для стыка) — `is_booked` в модели ПО-ПРЕЖНЕМУ НЕТ ([[triplanio-booked-flag-plan-fact]], код не начат; после его внедрения появится третье состояние «есть, но не подтверждено»).
- Состав: шапка-канон `.wdg-h` + бейдж `N/M`; прогресс (бар-пилюля 11px по форме `.bud-bar` ИЛИ кольцо-канон `.wring`); счётчики-чипы «Отели h/H» · «Переезды t/T» (тоны `--ev-hotel`/`--ev-transfer`); список слотов: занятый = `ListRow variant="raised"` (тайл типа + галка success + chevron) → клик открывает СУЩЕСТВУЮЩУЮ left panel просмотра (EventDrawerHost); пустой = `ListRow variant="add"` (пунктир, `--a` тон типа) → клик открывает СУЩЕСТВУЮЩУЮ fork-панель отелей (manual CTA + Stay22) / панель создания переезда. Done-state (всё забронировано) = success-плашка; empty-state (нет городов) = EmptyState с CTA «Открыть маршрут».
- На мобиле панели = bottom sheet (канон `.lp-sheet`), прогресс-бар вместо кольца где узко.

**Раскладка: Pavel выбрал «Сцену»** (из 4 предложенных «Эволюция/Штаб/Сцена/Сплит»; остальные ВЫПИЛЕНЫ из прототипа по его решению, итерация 2). Состав сверху вниз:
1. Карта-герой во всю ширину (360px), стат-полоса ПЛАВАЕТ на её нижней кромке (Card raised поверх), кнопка «Открыть карту» поверх правого верха; mobile: карта 230, статы НЕ поверх (под картой, 2 колонки).
2. Два столбца 1fr|1fr, РАЗВЯЗАННЫЕ по высоте (`align-items:start`, как живой `.ovwrap`): слева «Подготовка» компактным виджетом (сумма «N из M» + бар + группы «Отели»/«Переезды»), справа Бюджет + «Кто едет». ★Урок итерации 5 (жёсткое отклонение Pavel): у виджетов НЕЗАВИСИМЫЕ данные (и грузятся врозь) → равенство нижних кромок колонок — ЛОЖНЫЙ инвариант, его нельзя машинить (`stretch`/`flex:1`/прижатые CTA были костылями под него и давали «воздух» в карточках). Каждый виджет — своей естественной высоты; сомасштабность в типичном случае даёт сводочный кап, а не синхронизация.
3. Сервисы — лёгкой строкой: три add-плитки в ряд (eSIM · Аренда авто · Ещё), не колонка.
★ПРАВИЛА МАСШТАБА (итерация 4, требование Pavel «дизайн обязан работать при любом числе строк», не подгонка под демо): обзор = СВОДКА, полные списки живут в своих линзах. «Подготовка»: ≤3 слота на группу, НЕзакрытые первыми (они actionable), остальное за «Показать ещё N»/«Свернуть». Бюджет: топ-3 категории + агрегат «Ещё N категорий · сумма» (+серый хвост в баре); 0 категорий = пустая строка-канон. «Кто едет»: 3 ряда + агрегат «+N ещё». Карта-мок рисует пины от ЧИСЛА городов. В прототипе это ПРОВЕРЯЕМО: переключатель «Данные» Мини (2 города/1 участник/0 категорий) / Средний (4/3/4) / Макси (9 городов/8 участников/8 категорий) — раскладка держит все.
Mobile-порядок: карта(+статы) → Подготовка → бюджет → участники → сервисы (строка сервисов складывается в столбец).
⚠️Уроки итераций (Pavel, 2 отклонения): (2) три вольные колонки разной высоты под сценой = «стремновато»; (3) «Подготовка» ПОЛОСОЙ ВО ВСЮ ШИРИНУ («стена») ОТКЛОНЕНА ЖЁСТКО — это узор отвергнутого «Штаба», не возвращать. Отвергнута и сортировка слотов по маршруту (жила в «Сплите»).

**Файл:** self-contained HTML (токены = 1:1 срез app.css light+dark, Geologica, канон-примитивы Card/ListRow/Tile/Badge/statbar/wring; шелл-контекст: рейл+шапка+мобильный док). Отдан Pavel артефактом + файлом → положить в `Triplanio design new/OVERVIEW_REDESIGN_V2_2026-08-24.html`. Стенд: версии 1–4, тема T, экран D, состояния виджета (часть/ноль/всё/пусто), клики по слотам открывают моки панелей.
