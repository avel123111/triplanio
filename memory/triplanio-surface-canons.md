---
name: triplanio-surface-canons
description: Канонические поверхности (карточки/панели) - token-driven, ровно 2 роли glass/panel (TRIP-189)
metadata:
  type: project
---

★TRIP-189 (PR в dev 2026-07-04): все карточно-панельные поверхности сведены к
ЕДИНОМУ источнику - пять токенов в `src/design/app.css` и ровно две роли-скина.

Токены (`:root` = светлая, `:root[data-theme="dark"]` = тёмная, JS не участвует,
всё декларативно через `data-theme` на `<html>`):
- `--r-xl: 20px` - единый радиус всех поверхностей (был разнобой 24/20/16);
- `--surface-card` - фон: светлая `rgba(255,255,255,.72)` (слегка прозрачный,
  чтобы blur давал матовое стекло и в свете), тёмная `rgba(255,255,255,.07)`
  (стекло поверх `--bg` #0c0e1c);
- `--surface-card-border` - бордер: светлая `rgba(45,55,115,.22)`, тёмная
  `rgba(255,255,255,.17)`;
- `--shadow-glass: 0 18px 50px -26px rgba(0,0,0,.5)` - тень только у роли glass;
- `--glass-blur: 16px` - backdrop-blur только у роли glass.

Две роли назначаются ПО ЭКРАНУ (не глобально по классу), групповыми
селекторами в app.css - единственное место со скином:
- `.surface-glass` - «плавающая»: тень + backdrop-blur. Все поверхности `/trips`:
  `.statbar` (Города/Страны), `.wmini` («Мир исследован»), `.nextcard`,
  `.nonext`, `.tr` (строка трипа list-view), `.mapwrap` (карта-герой - фон
  остаётся wash под картой, но тень/бордер/радиус как у стекла).
- `.surface-panel` - «плоская»: без тени и блюра. Экраны трипа/статы/аккаунта/
  редактора: `.card`, `.statcard`, `.wdg`, `.panel`, `.invite`, `.acc`,
  `.acct-hero`, `.pl-lastcard`, `.lpanel`, `.summary` (полоса 6 стат-плиток на
  /stats), `.docfield` (последний - в DocumentsField.css, на тех же токенах
  напрямую).

Принцип: на ОДНОМ экране все поверхности одной роли (иначе мешанина - именно
из-за этого Pavel видел «больше 2 стилей»: стеклянная statbar рядом с плоским
виджетом на /trips).

Правила поддержки: примитивы НЕ дублируют скин - подключаются к роли через
групповой селектор, а в своём блоке держат только внутреннюю раскладку
(падинги/хедеры/overflow). Новую поверхность вешать на `.surface-glass` или
`.surface-panel`, не переопределять фон/бордер/радиус руками. Глобальные
`--surface`/`--line` НЕ трогали - инпуты/меню/чипы/строки остались как были
(иначе «остеклился» бы весь UI в тёмной теме). Карточки-постеры трипов `.tc`
(фон-картинка + скрим) вне ролей: выровнен только радиус на `--r-xl`, тень/фон
свои. Прочие вне ролей (осознанно разные примитивы): `.limitcard` (брендовый
алерт лимита), `.tl3-card` (карточки эвентов таймлайна - защищены отдельным
фидбэком [[feedback-redesign-means-from-scratch]]), строки-списки (`.bookrow`,
`.bgt-exrow`, `.acct-chan`…), вложенные recessed/inset-блоки на `--surface-3`
(`.route`/`.addr`/`.notes`/`.doc-row`…), контролы (инпуты/кнопки/сегменты) и
оверлеи (`.toast`/`.sheet`/`.chat-mention`/диалоги). Имена `.tp-glass-card`/`.tp-panel`/`--card-border`/`applyLook()` из тикета -
из HTML-прототипа `Triplanio.dc.html`, в реальном React/Vite-репо их нет.
Усиливает [[feedback-reuse-first-unification]] и [[triplanio-typography-canons]].
