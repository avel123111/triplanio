- Triplanio: эпик «единая дверь в данные» (TRIP-374) — ★★ЧИТАТЬ ПЕРЕД ЛЮБОЙ ПРАВКОЙ ДОСТУПА К ДАННЫМ. Handoff — в Linear-документе (единый источник, в memory не дублируется): https://linear.app/triplanio/document/handoff-epik-edinaya-dver-v-dannye-trip-374-8720de36a6ec . Цель: edge — единственный дом авторизации, БД под `service_role`, RLS удаляется как авторизация (в БД только целостность FK/CHECK/UNIQUE/транзакции), фронт в БД не ходит (КРОМЕ логина / Storage-байтов / realtime чата / газеттира — намеренно разрешённые прямые обращения, НЕ мигрировать и НЕ зарубать). ГЛАВНОЕ ПРАВИЛО: число endpoint-ов от задач экрана, НЕ от числа вызовов (если #новых endpoint == #убранных вызовов — проектирования не было, PR возвращается); чтение группируется ПО ЭКРАНУ, запись ПО РЕСУРСУ. Решение = Вариант B (авторизация уходит из RPC в edge, RPC берут явный `p_actor`, `mutate.ts` однорежимный service_role). `_can_edit_trip` НЕ удалять (гейтит прямой Storage). Актуальные числа — `npm run check:door`. Все решения/статус/фазы/ловушки — в Linear-документе.
- [[triplanio-full-height-surfaces]] — TRIP-494: полноэкранных поверхностей ДВЕ (шторка пикера и оболочка панелей редактора) и это ОДНА вещь; роль «я экран» объявляет ШОВ признаком `data-sheet-full` (класса `.sheet--full` больше нет — он был вторым способом сказать то же), из признака следуют коробка, краска, бровь и резерв под клавиатуру. Краска — цвет СТРАНИЦЫ `--bg` (карточка панели внутри её отдаёт): до сведения шторка красилась цветом КАРТОЧКИ, и карточки внутри неё совпадали с фоном байт-в-байт. Скроллер называет себя сам (`{...sheetScroller}`), семья решает, кому это важно. Ловушки: `.lp-sheet` — флекс, и бровь как второй ребёнок встаёт СЛЕВА без явной колонки; **способ въезда — свойство поверхности (`pinned`), а НЕ запрос по её содержимому**: живой `:has(.ss-search)` отваливался на второй фазе композера и vaul проигрывал въезд ЗАНОВО («закрылся шит, открылся второй такой же с плитками»); 2p ведёт учёт по именам классов — снятый класс стоит маркера на каждое объявление. **открытость поверхности — ФАКТ, а не «отрисован ли элемент»**: литерал `open` при стопке слоёв давал залипание шторки на полпути (vaul закрыл, экран не заметил); дисмисс закрывает ПОВЕРХНОСТЬ, «назад» снимает слой. Приёмка — `npm run check:surfaces` (21 проверка, стенд `/kit/full-surface`).
- [[triplanio-wizard-route-model]] — TRIP-484 §4: маршрут визарда создания сведён к ОДНОМУ списку узлов с `kind` (как в route lens); `home`/`cities`/`finishCity` стали выводом, `isStay` не стало вовсе, композер города `cities/CityAdder` общий на визард и редактор, на обеих платформах. Правила вставки от редактора (старт в начало, финиш в конец, остальное ПЕРЕД финишем) — «финиш последний» свойство построения. Ловушки: цепочку дат решает `nights != null`, а не `kind`; **«останусь» = ОТСУТСТВИЕ узла `end`** (пометить им город значило завести второй вид финиша), **дефолта «домой» НЕТ** — возврат существует только явным узлом, иначе «не выбрал» молча значит «домой»; со ступени, на которой стоишь, не выселяют (`step !== 'return'`); канал факта наружу обязан переживать РАЗМОНТИРОВАНИЕ (эффект ушедшего узла не бежит — закрывает уборка). Гейт — `routeModel.test.js`, 17 проверок, каждая увидена красной. **Телефонный флоу композера: ОДНА поверхность с двумя фазами вместо двух вложенных шторок** (3 тапа вместо 4, фокус в поле с первого); механика — `Autocomplete embedded` + `PickerSheet full`. **Выбор строки пикера — жест `pointerdown`/`pointerup`, а не `click`** (WebKit съедает его коммитом композиции от подсказки клавиатуры), порог общий с `useRouteDnD`, чинится сразу у обоих движков.
- [[triplanio-trip-shell-one-round]] — обвязка экрана трипа (шапка+меню) собирается из ПЕРВОГО ответа: `getTripDetails` отдаёт `myStep` (ту, что сам проверил) и `isPro` (`is_trip_pro`), право течёт только через `TripAccessProvider`, FE-копия правила о ролях удалена; рейл рисует негейтованные пункты живыми с первого кадра (`loadingSections`); с главной меню открывается ГОТОВЫМ — карточка (`['trip-card', id]`) везёт `myStep`+`addons`, реестр принимает факты, а не трип. Ловушка: у любого edge-вызова платформенный пол ~350–400 мс (getMe min 354, JWT-проверка всего 15 мс) — оптимизировать содержимое функции нечего; делить ответ ради «лёгкой первой порции» значит платить круг; зеркало из кэша `/trips` = второй источник правды (класс TRIP-277).
- [[triplanio-site-design-system]] — TRIP-446 Ф1 + TRIP-447 Ф2: сайтовая ДС (неавторизованная зона). Ф1: `public/site.css` (бывш. `landing.css`), токены на `html.site` а не голом `:root` (класс вешает `useSiteCss()`; `/terms`+`/privacy` несут `class="site"` в разметке), примитивы без префикса (`btn`/`header`/`footer`/`container`/`eyebrow`), гард 2ac `check-site-prefixes` (ратчет namespace, БАЗА резолвит site.css→landing.css, хост `.site` вне счёта). Ф2 (`SiteChrome.jsx`): ОДНА шапка/подвал, состав пропом `variant` (`full`/`cta`/`minimal`); тема шапки `on-light|dark|accent` по `[data-hdr]` секции (проп `themed`, лендинг, пересчёт скролл+монтирование+маршрут, `dataset.hdr` без проверки, `on-accent` производитель = финальный CTA `data-hdr="accent"` с TRIP-460, контраст шапки доведён до AA); **имена классов прототипные, переносятся как есть (TRIP-460, отменяет решение Ф2 о репозиторных именах)** — ≈124 namespace-семьи прототипа под ОДНИМ `prefix-exempt` (апрув Pavel 24.08.2026), 7 реальных коллизий с `app.css` разрешены вариантом/переименованием (`.brow`/`.sheet-pane`); кнопка через `withVisitCampaign()`+`nav()`; гард границы ДС `check-ds-boundary` (в CI с Ф9). Правило «старый CSS умирает вместе со страницей» в Ф6. TRIP-456 Ф2а: атрибуция под гардами — 2j расширен D (единственный `signUp` несёт `signup_attribution`) и E (провайдер OAuth предваряется `rememberAttributionForRedirect`), новый 2ad `check-site-nav` (внутренний `<a href>` в сайтовой зоне = потеря метки, обход `nav-exempt` у `/terms`+`/privacy`); `entrySearch`-снимок, `withVisitCampaign` ×4, PostHog грузится ДО согласия («ноль байт» неверно). **TRIP-460 Ф6.1 (лендинг в dev):** ступени `--fs-*` — ЦЕЛЫЕ px, сырая величина под новым именем это не шкала; «сдвиг ≤1px невидим» ОПРОВЕРГНУТО — он переворачивает разбивку строк (сверять по МАКЕТУ на ru); периметр 2p расширен на `public/` (ранг site.css=1000, выигрывает у app.css и index.css — оба конца запинены тестами); приёмка = `npm run check:proto` (+`playwright-core` в devDeps, эталон вложением в Linear, в CI не будет), 390 обязательна и хуже 1440; попиксельный диф НАСЫЩЕН на полноэкранном градиенте (90–96% на любой правке цвета) — контраст `.final` решать глазами, он открыт (2.18/2.29 при 3.0/4.5). **TRIP-455 Ф10 (сквозная приёмка):** переполнение 0, тёмная тема 5.25–16.34, фантомных табстопов 0, сырых ключей 0, зона нажатия — 0 нарушений вне бегущего текста (44px оверлей одним зонным списком, подвал адресуется `> *`). Ловушки замера: баннер согласия перекрывает ВСЁ (21 элемент вместо 62), `scroll-behavior:smooth` врёт координатами, закрытый `<details>` отдаёт потомкам rect контейнера, SPA-фолбэк стенда рождает 144 ложных JS-ошибки; зону нажатия мерить `elementFromPoint`, НЕ rect. Открыто: `.bud-bar` под `site-dup-exempt` (белый список из 6 сбросов), One Tap, Lighthouse.
- [[triplanio-stacking-layers-contract]] — TRIP-466: контракт слоёв — «слой называет свой уровень» (`z-index` у полноразмерной абсолютной подложки) + «кадр владеет слоями» (`isolation: isolate`; `position: relative` контекста НЕ создаёт). Прод-баг: обложка планировщика на iOS накрывала скрим/стрелки/НАЗВАНИЕ ТРИПА — ровно при смене фоллбека (CSS-фон, не элемент) на `<img>`, который движок промоутит в композитный слой; z-index при этом не проигрывает, а не участвует. Карточка `.tc*` контракт несла (img 0 / скрим 2 / контент 3), при переносе в примитив `<Cover>` перенесли скрим и контент, слой картинки назвать забыли. ГЛАВНОЕ: чинить надо КАДР, а не примитив — общий пин `.cover__img{z-index:0}` в WebKit поднимает фото над `::after`-накладкой `.nextcard__cover` (Chromium — нет), то есть чинит один экран и ломает другой; фикс = `.tcp__hero{isolation}` + `.tcp__track{position:relative;z-index:0}`; пинить надо ТОТ элемент, который движок промоутит (скроллер), а не то, что внутри него — пин слайда/картинки этот случай не покрывает. Ловушки: десктоп и Linux-WebKit сам баг НЕ воспроизводят — проверять запертость (`elementFromPoint` под ребёнком z-9999); первый кадр Playwright всегда отличается независимо от CSS — без прогрева попиксельная сверка врёт. Гард 2af `check-layers`.
- [[triplanio-share-card-map]] — TRIP-443: карта share-карточки. Своего облика у неё БОЛЬШЕ НЕТ — те же Ring-пины и канонные веса линий, что у живых карт; общие правила (нумерация транзитов `cityPoints`, размер по зуму `markerZoomScale`/`markerZoomSizeExpr`) живут в `markers.js` рядом с обликом. Вынужденная разница одна: снимок берётся с canvas, DOM-маркер туда не попадает, поэтому пин — GL-иконка, и `pinImage.js` не описывает пин заново, а ИЗМЕРЯЕТ настоящий `.tmk` (ни одного размера и цвета пина в коде). Плечо БЕЗ переезда карточка рисует ЦЕЛОЙ сплошной, а не приглушённым пунктиром: пунктир — предупреждение о дыре в плане, а карточке предупреждать не перед кем; правило `legLook(kind, markGaps)` в `mapStyle.js` зовут все три места, где решался облик (раньше три копии `!kind`), и облик входит в подпись кэша линий. Тема КАРТЫ ≠ тема приложения: цвет чужой темы берётся из ПРАВИЛ (CSSOM `collectThemeVars`), потому что `:root[data-theme=dark]` вложенному пробнику не достать; цвет линий доводится на каждом заходе — кэш линий сравнивает геометрию и на смену темы не реагирует. Вес метки задаётся под размер, В КОТОРОМ НА ПОВЕРХНОСТЬ СМОТРЯТ: живую карту смотрят 1:1 (вес 1), карту карточки РЕНДЕРЯТ крупнее, чем на неё смотрят, поэтому `markerSurfaceWeight = max(1, ширина/420)` (слот стори ×1.93) — иначе те же 29 px весят вдвое меньше канона (замер: пин 3.3% ширины карты против 6.5%, линия 0.43% против 0.90%); зум-диапазон этого НЕ лечит и линий не касается. Диапазон при этом сужен 0.6→0.85: 0.6 бил не по «карте мира», а по обычному трипу (3 города по Италии = zoom ≈ 4.6, почти нижняя полка). ЛОВУШКИ: «скрим над картой в превью» = ТЕНЬ ПОЛАРОИДА, просвечивающая сквозь дыру окна (замер 204 против 255) — всё под окном в overlay видно насквозь; толщина скошенного шва пилюля — из `offsetWidth`, охват шире полоски на величину скоса; `icon-allow-overlap: true` обязателен, иначе города молча пропадают; кадр редактора обязан идти в пропорции СЛОТА (на мобиле full-bleed давал «настроил одно, получил другое»).
- [[triplanio-share-card-text-layout]] — TRIP-443: текст share-карточки. ГЛАВНОЕ: белый текст лежал на ПРОИЗВОЛЬНОМ фото без подложки — города «пропадали» не от шрифта, они СЛИВАЛИСЬ (видны те доли секунды, пока новое фото не отрисовалось; «тень» у цифр = тёмная копия withShadow, проступающая на светлом кадре). Базовый градиент сам светлый сверху (1.36:1), то есть баг ловился и БЕЗ фото. Фикс — скрим канона `.tc__scrim`, форму задаёт РАСКЛАДКА (проценты сползают с текста молча), ALPHA=0.55 выведена из порога контраста; худшая строка 1.00→4.11 (белый фон), 1.47→5.75 (градиент). Ещё: раскладка переведена с догадки `длина×кегль×0.54` на таблицу реальных ширин `glyphWidths.ts` (стрелка садилась на букву), подписи склоняются, overlay больше не вшивает шрифты (одно имя — один источник лиц; 116→14 КБ), `card_svg` вшивает обязательно. ЛОВУШКИ: `fonts.ready` ≠ «шрифт применён», одиночный пробел меряется как 0, без `unicode-range` латиница меряется фоллбеком, кегль ≠ высота рамки глифа; сход скрима по smoothstep на 0.18 высоты (короткий линейный имеет два излома и читается ПОЛОСОЙ), длиннее нельзя — упрётся в окно карты. ★Проверять карточку на Vercel-превью НЕЛЬЗЯ — её рисует edge-функция, превью деплоит только фронт.
- [[triplanio-brand-mark-assets]] — бренд-марка Triplanio (TRIP-479): ЕДИНСТВЕННЫЙ источник геометрии `public/triplanio-logo.svg` (непрозрачная плитка `#2173C8` + белые path СВЕРХУ, viewBox 192) — из него favicon.ico/apple-touch/icon-192/512 и base64 share-карточки; `icon-512.png` ещё и `Organization.logo` в JSON-LD (Google не берёт SVG). Вторая копия геометрии — символ `#tl-logo` в `LandingSprite.jsx`, едет ЗА мастером. Ловушка viewBox: `SiteChrome`×2 и `LandingPage`×3 задают его сами (править вместе), а `AuthShell` и строка перевода `landing.pn.divider` — нет и берут из символа, поэтому смена марки НЕ трогает локали (и Tolgee). Грабли: knockout-конструкция «квадрат с дыркой» чернеет на тёмном (не возвращать); конвертеры SVG→ICO плющат альфу на белое; `apple-touch-icon` — единственный, кому прозрачность ВРЕДНА; `vercel.json` держит пофайловый список Cache-Control. НЕ сведены: maskable-иконки нет (Android режет хвост самолёта), og-cover/og-join — марка запечена в фотоколлаж; вне репо — письма Supabase Auth и аватар TG-бота.
- [[triplanio-gazetteer-search-two-phase]] — TRIP-491: поиск города = ТРИ фазы (отбор двумя ветками по `population` → полное ранжирование ~350 → `gaz_project` только для финальных `lim`); 6 625 → 129 мс на проде, на dev подтверждено 7 900 → 242 мс на одной машине; индексы были здоровы всё время. Ловушки: цена зависит от длины ПЕРВОГО токена (разработчик вводит имя целиком и всегда в дешёвой ветке), два расхода спрятаны в КЛЮЧЕ сортировки (`log(numeric)` + повторный tsvector-матч), живые функции в БД расходятся с миграциями (брать `pg_get_functiondef`), тревога про холодный кэш СНЯТА сквозным замером (буферы 685 289 → 27 324, в 25 раз МЕНЬШЕ — опасение касалось только фазы 1 в отрыве от целого).
- [[triplanio-trip-cover-presets]] — обложки трипа: пресеты (публичный бакет `trip-cover-presets` + таблица `cover_presets` ярус D, читаются через edge `getCoverPresets`) + своё фото; градиенты выпилены. Фоллбек «нет обложки» = картинка в РЕПО `public/covers/fallback.webp` (`COVER_FALLBACK`). Выбор пресета КОПИРУЕТ URL в `cover_image_url` → удаление пресета ничью обложку не ломает. Курировать без деплоя: залить в бакет + самонаполняющийся `insert` из `storage.objects` (скрипт в файле). Ф2 выпилила колонку `cover_gradient` (4 RPC переопределены минус колонка + `telegramGetMyIntegrations` + DROP + регенерация типов 2t). Грабли гардов 2t/2p/2l/2r — в файле. Фоны share-КАРТОЧКИ — отдельный бакет `card-bg-presets` БЕЗ таблицы (ссылок на фон не существует): листает edge `render-share-card` → `backgrounds` overlay-ответа (формат = папка story/|post/ внутри бакета); своё фото юзера как фон НЕ сохраняется (эфемерно).
- [[triplanio-ds-share-metric]] — TRIP-337 §1: главное число эпика — доля UI-элементов из ДС 23.09%→≥60%, предикат `audit-design.mjs` §1e, поле `dsShareBp`, 9-я строка пола 2o. Ловушка: внутренности ДС и своя композиция ВНЕ знаменателя (иначе штрафует правильную работу); составное имя JSX = всегда компонент.
- [[triplanio-data-door-board-2r]] — TRIP-376: гард 2r `check-data-door.mjs` — ратчет на 9 числах эпика «единая дверь», свежие всегда `npm run check:door`. Ловушка: RATCHET (блокирует) ≠ TARGET (печатается, красит лишь под `--assert-target`); grep построчный, регулярка JS идёт через `\n`.
- [[triplanio-booking-expense-mirror]] — TRIP-484: зеркало «бронь → трата» пишет ТОЛЬКО триггер БД, а клиенту его отдаёт шов (флаг `returnExpenses` на 9 booking-действиях, ответ = конверт `{row,cities,transfers,expenses}`, сворачивает одна `reconcileBookingWrite`); та же дыра во втором пути — каскад удаления города (`pruneCityContent`). Плюс закон дат `lib/dateRange` (монотонная цепочка, вперёд и никогда назад), якорь месяца календаря и выпил нативного `.select`. Ловушка: забытый флаг ничего не ломает на записи — бюджет молча отстаёт, поэтому инвариант пинится тестом.
- [[triplanio-onboarding-emails-pipeline]] — письма онбординга (welcome + 2 догонялки): текст едет Tolgee → n8n **Data Table** (не живым запросом), `Flatten` режет ключ ПО ПЕРВОЙ ТОЧКЕ, поэтому префикс ключа = имя события = фильтр таблицы = путь вебхука. Все три письма — топик **Onboarding**, топик это параметр ОТПРАВКИ, а не свойство шаблона. Отписка и `List-Unsubscribe` — НАШИ (TRIP-513): у `/emails` Resend списками не управляет, обе двери упираются в id контакта Resend. Имя человека и имя трипа — склейкой в ФИКСИРОВАННОЙ позиции, плейсхолдера внутри перевода нет; `<title>` письма темой НЕ является. Ловушки: `Tolgee sync` только РУКАМИ, **upsert НИКОГДА не удаляет** (переименование ключа в Tolgee оставляет старую строку в таблице навсегда — уже стоило 10 сирот), `common.*` и событие делят одно `t[lang]`. Старый welcome: шаблон удалён из Resend, нода отправки на него ВЫКЛЮЧЕНА (ничего не падает, welcome просто не идёт), настроена на топик Marketing и отписку на несуществующий `/settings`; `Create a new contact2` рядом ЖИВАЯ — единственный источник контактов Resend, и встраивать её надо ЛИНЕЙНО перед отправкой (её ответ везёт `id` контакта для отписки).
- [[triplanio-entitlement-notify-transition]] — уведомление о Pro приезжает на ПЕРЕХОД права (RPC `recompute_*_entitlement` возвращают boolean под `for update`), одна дверь `_shared/entitlementRecompute.ts`. Прод: 0 «Pro активирован» на 5 подписок — notify висел на ОДНОЙ ветке checkout, а строку подписки рождают ЧЕТЫРЕ пути (улика: `provider_ref = null`). Ловушки: первый инвойс (`billing_reason=subscription_create`) — это 3DS, а не дённинг (уведомление ушло за 13 сек ДО оплаты); тем же корнем терялась выручка `purchase_completed`; гонка вебхуков реальна — 300 мс.
- [[triplanio-honest-refusal-answers]] — TRIP-378 Ф0.2: отказ приезжает HTTP-статусом (402/403/404/500), не 200/404-заглушкой; тронуты `deleteTrip`/`updateTripSettings`, тексты через `refusalToast`, гейт `edgeError.test.js`. Ловушка: смена статуса заводит Sentry-события (лечится `x-sentry-skip`); серверный текст пользователю НИКОГДА.
- [[triplanio-ts-check-ratchet-2s]] — TRIP-377 Ф0.1: храповик покрытия `// @ts-check`, гард 2s `check-ts-check.mjs`, идёт ВВЕРХ (снятие прагмы зеленит `typecheck`, регресс выглядит успехом). Ловушка: предикат = семантика TS (9 файлов), не греп (22); конфиг не-`jsconfig.json` теряет noEmit и эмитит .js в дерево.
- [[triplanio-db-types-drift-guard-2t]] — TRIP-383 Ф0.5: типы схемы `_shared/database.types.ts` (руками не править, воспроизводимый вывод `supabase gen types`) + гард 2t `check-db-types.mjs`, джоба db-types. Ловушка: сверяет МИГРАЦИИ↔ТИПЫ на своём postgres (не живую dev), правку схемы мимо миграции не видит; один генератор на обе стороны.
- [[triplanio-spacing-ratchet-and-css-cascade]] — TRIP-339 Ф1: ярус SPACING = мультимножество сырых отступов по всему репо (1323 сырых/46 уникальных), детерминированный каскад (`app.css` первый, `index.css` последний, порядок в `main.jsx`). Живой дефект: `index.css:211–219` = 2-й канон типографики. Предикат раньше числа.
- [[triplanio-tint-single-channel]] — TRIP-341 PR3: тинт сведён в один канал `--hl*`, `--primary-soft*` удалён (163→161), классов 1341→1332 (первый минус эпика). Ловушка: у общего имени появляются ЧУЖИЕ читатели по наследованию (`.fork-pg--on`), невидимо гардам — перечислять всех читателей+сеттеров, пинить канал на объекте.
- [[triplanio-css-alias-frozen-at-root]] — TRIP-341/352: `--X: var(--Y)` в `:root` вычисляется НА `:root`, переопределение `--Y` на потомке (`.auth`/`.ptrip`) `--X` не трогает (тема — не этот случай). Следствие: у читателей `--hl*` фолбэков нет, мёртвую ветку `var()` не видит ни один гард.
- [[triplanio-design-floor-guard-2o]] — TRIP-338 Ф0: гард-пол 2o `check-design-floor.mjs` — храповик на всех числах сразу (классы 1341/namespaces 124/инлайны 694/токены `:root` 163), предикат Р8: токен = имя в `:root`. Ловушка: понижение токена переносом из `:root` выглядит прогрессом (имя спряталось); escape `floor-exempt: <метрика> +N` живёт один PR.
- [[triplanio-design-system-work-rules]] — TRIP-321: ★★ЧИТАТЬ ЦЕЛИКОМ ДО ПЕРВОЙ ПРАВКИ ДС. Единственная метрика приёмки — число КЛАССОВ (`audit-design.mjs`), мерить до/после; ничего не заводить заново, работать по ОБЪЕКТУ. Ловушки, невидимые гардам: несуществующий класс=тишина, осиротевшее правило, прогон по значениям сделал чекбокс круглым.
- [[triplanio-input-fields-unification]] — TRIP-333: поля → `<Input>`/`<Textarea>`/`<InputGroup>` (`src/design/Input.jsx`), высота токен `--ctl-h` 40/44 через `min-height`, обязательность решает ВАЛИДАТОР (`isFieldRequired`), знак один `[data-required]::after`. Ловушка: правило по атрибуту (`input[type=time]`) бьёт класс владельца невидимо.
- [[triplanio-member-identity-seam]] — TRIP-334: «кто человек» = 2 шва — фронт `resolveAuthor` (5 ступеней) + бэк `fetchTripProfiles` (`_shared/profiles.ts`): охват `tripProfileScope` ≠ право `liveIdentityIds`. Прод-баг: фильтр `status='active'` рисовал прочерк `-`. Опознать ≠ показать; гейт=тесты на чистых функциях.
- [[triplanio-graphify-code-graph]] — `/graph` (graphify, tree-sitter, 0 LLM-токенов, 8 c): `affected`/`explain`/`god-nodes`, файловая точность = греп. Ловушка: «нет входящих ссылок» ≠ «мёртвое» (199 ложных сирот в src, 43/45 edge-функций); пустой `affected` на одноимённых = «проверь имя». Не детектор мёртвого.
- [[triplanio-design-unification-trip321]] — TRIP-321 диагноз (PR #634): 4600 объявлений, 1334 класса, 261 префикс, 5.7% компонентов ДС; 37 форм размножены по namespace, примитивов раскладки НЕТ. Ловушка: массовый прогон по значениям ломает живое (чекбокс→круг); мерить ПРЕФИКСЫ, а не значения.
- [[triplanio-upload-format-hardening]] — TRIP-281: гейт формата = `storage.buckets.allowed_mime_types`, не `accept=` (DnD его минует); шов `uploadTripFiles`, словарь `src/lib/fileType.js`. Ловушка: React 18 НЕ блокирует `javascript:` в href → stored XSS, лечит `normalizeExternalUrl`+CHECK; режем HTML/SVG. TRIP-367: публичный бакет = ноль SELECT-политик (гард 2e/TRIP-48), поэтому перезапись НЕ через `upsert` (тянет `INSERT ON CONFLICT → SELECT` → `new row violates RLS`), а уникальный ключ на версию (`<uid>/<uuid>`) + указатель в профиле + best-effort снос старого.
- [[triplanio-cookie-consent]] — TRIP-311: `posthog.init()` только из `consent.js` при согласии, до ответа ноль байт; гард 2j `check-analytics-seam.mjs` (3 правила «одна дверь на сторону»). Ловушка: до согласия придерживаются 4 вещи вкл. САМИ СОБЫТИЯ (`pendingEvents`); метки едут ОДНИМ носителем на границу.
- [[triplanio-ad-attribution-layer]] — TRIP-316: `user_signed_up` в единственной точке рождения юзера (создание строки `public.users`), метка кампании `camp_*` (не utm_*), `src/lib/campaign.js`. Ловушка: `identify` живёт в ОДНОМ месте — кто создал персону, решил канал навсегда; `$initial_utm_*` третьим аргументом `identify`.
- [[triplanio-viral-link-marking]] — TRIP-329: 4 виральных ссылки метятся одним хелпером (`viralLink.js`+`.ts`), `utm_campaign=trip_<id>`, префикс `viral` в medium. Правило: метим ВИД ссылки, не получателя. Ловушка: метка едет В АДРЕСЕ → перезагрузка документа теряет её, лечится `nav()` вместо `location.href`.
- [[feedback-ui-copy-must-fit-its-container]] — TRIP-277: длина UI-строки = ВЁРСТКА; переименование `event.ai_reset` сломало флекс-ряд `.ai-blk-hd`. Правило: перед правкой строки грепнуть ключ, открыть JSX+CSS контейнера (nowrap/min-width:0), мерить по длинной локали; откат в JSON И Tolgee.
- [[triplanio-tripster-cities-seed]] — TRIP-236 (PR #535): фид Tripster (n8n-вебхук, 942 города) → cities; дроп мёртвых гео-колонок + `tripster_slug`(CHECK≤128) + fork-ссылка на слаг + сид 260 РФ-городов; резолв через газеттир pop-first. Мис-мапы вычищены.
- [[triplanio-chat-lens-redesign]] — TRIP-296 (PR #591): запись сообщений только через secdef-RPC `send_chat_message` (ярус C→B), `mentions_assistant` в SQL, «Triplanio печатает» = серверный прогон `ai_*` + `pg_cron` watchdog; линза = полноэкранная комната, история последние 200 (`DESC+limit+reverse`). Непрочитанные: счёт из БД (`chat_reads.last_read_at`), отметку двигает ОДИН хук `useMarkChatRead` на обе поверхности — копия правила «когда отмечать» в виджете копила непрочитанные при открытой панели.
- [[triplanio-sentry-coverage-plan]] — TRIP-219: охват Sentry edge (гард 2h)+фронт (2i) завершён, severity-4xx через `x-sentry-skip`; n8n шлёт в Sentry напрямую; выпил `getDailyReminders`. Мониторинг поймал 1-й реальный сбой (edge 503→reminders)→TRIP-242.
- [[triplanio-input-integrity-db-layer]] — TRIP-169: целостность ввода = слой в БД (не edge-валидатор — покрывает все 3 пути записи): домены short/long/url_text + char_length CHECK + `>= 0`; гард 2g `check-column-caps` не пускает сырую text-колонку без кэпа. Отклонён zod/кодоген.
- [[triplanio-ponytail-vendored]] — TRIP-218: ponytail (MIT, 7-ступенчатая лестница минимизации кода) вендорен в `.claude/skills/ponytail*` (4 скилла), не через `/plugin` (self-hosted Cyrus его прячет); мандат CLAUDE.md rule #6.
- [[triplanio-marketing-hub]] — TRIP-198: указатель на маркетинг-контекст; ОТКРЫВАТЬ файл ТОЛЬКО по явной просьбе Ильи/Pavel, без просьбы содержимое не подтягивать.
- [[triplanio-marketing-psychology]] — TRIP-194: справочный слой триггеров (скилл marketing-psychology), применяется при копирайтинге; 4 несущих: избегание потери (главный), честная ургентность (реальный дедлайн), принадлежность, соц-пруф (не фабриковать). Полный файл в Linear.
- [[triplanio-content-strategy]] — TRIP-200: контент-стратегия — 5 пилларов, 3 канала (SEO+шаблоны и Telegram RU co-#1), редкалендарь по AARRR, демо-магия вместо соц-пруфа. Флаг: ценность TG-бота не валидирована (P3 гипотеза). Деливери в Linear.
- [[triplanio-launch-offer]] — TRIP-184: оффер Pro поверх голой цены; рычаг = risk-reversal (freemium+отмена в 1 клик+бессрочный pro_trip); Илья убрал возвраты и реферал. Блокер: Stripe не сеттлит RUB → платный оффер для RU заблокирован.
- [[triplanio-surface-canons]] — TRIP-189→326: все поверхности сведены к 5 токенам (`--r-xl:20`, `--surface-card`, `--surface-card-border`, `--shadow-glass`, `--glass-blur`) и ОДНОЙ плоской роли (glass/panel = алиасы); глобальные `--surface`/`--line` не трогали.
- [[triplanio-function-search-path-convention]] — TRIP-54 (PR #376): все 54 функции `public` → `SET search_path = public, pg_temp` (pg_temp последним). Ловушка/TRIP-49: least-privilege ревок шёл `FROM anon` при гранте на `PUBLIC` → фикс `REVOKE FROM PUBLIC`, пост-деплой `has_function_privilege('anon',…)=false`.
- [[feedback-dead-i18n-key-sweep-must-scan-backend]] — TRIP-299: «мёртвые» i18n-ключи нельзя вычислять сканом только `src/**` — ключ уведомления в БД (`notifications.i18n_title_key`, пишут DB-триггер+6 edge); 22 живых удалили. Источников ссылок ТРИ: src+functions+migrations; форсит гард 2d (C бэкенд + D фронт-литералы).
- [[feedback-new-i18n-keys-go-to-tolgee]] — TRIP-176: `tolgee pull` в CI перезаписывает locales JSON → ключ, которого нет в Tolgee, пропадает (сырой ключ на экране). Правило: новую/изменённую строку заводить В Tolgee (project 2, en/es/ru), не только в JSON.
- [[triplanio-country-flag-primitive]] — TRIP-177 (PR #353): эмодзи-флаги стран → SVG `public/flags/<iso2>.svg` через единый `CountryFlag` (`.cflag` height:1em); выпилены `countryFlag`/`flagEmoji`/мёртвое `LANGUAGES.flag`.
- [[triplanio-typography-canons]] — TRIP-165/175/183: весь текст сведён к 10 текст-стилям `.t-*` (единый источник через co-selector), гард `check:design` BLOCKING; TRIP-183 миграция на Exo 2/Golos/JetBrains Mono + display-ручки `--fd-w/--fd-k`.
- [[triplanio-cities-seed-only]] — TRIP-135 (PR #227): фикс RLS-краша сохранения трипа (`set_city_id` resolve-only, `learn_city` дропнут → cities не пишется из рантайма) + унификация Pro-статуса `useProStatus` (вердикт из кэша, плашка не падает в Free).
- [[triplanio-personal-docs-teardown]] — TRIP-44 (PR #216): личные `trip_documents` (private) удаляются (строки+Storage) при выходе/удалении участника (`removeTripMember`) и аккаунта; единый `_shared/personalDocsTeardown.ts` (best-effort). Ловушка: миграция переехала на 20260627230000 (баг out-of-order деплоя).
- [[triplanio-map-shell-insets]] — TRIP-422/484: одна раскладка «карта + виджет/шит» на три экрана (`MapShell`). НЕСУЩЕЕ: холст ВСЕГДА во всю площадь и НИКОГДА не меняет размер — ось решает, кто уводит кадр: панель режет ширину → отступ КАМЕРЫ, шит режет высоту → СДВИГ холста на половину закрытого (`padding` на globe рисует диск = «круги вокруг глобуса»), потолок сдвига — второй сверху детент. Три ловушки, каждая стоила дня: ресайз холста двигает мир на половину дельты и роняет шар; команда камере с ТЕМ ЖЕ отступом обрывает летящий автофокус (`padUnchanged`); высота окна — часть цели кадра, ширина нет (`fitHeightSig`), иначе точки уходят под шапку и шит. Два отступа mapbox (фит = воздух+закрытое, поверхность = только закрытое) путать нельзя, `map.fitBounds()` их СКЛЕИВАЕТ и молча портит состояние карты; `setPadding()` = рывок, `jumpTo` игнорирует `offset`. Скриншотного гарда у геометрии НЕТ.
- [[triplanio-visual-check-playwright]] — TRIP-282: геометрия = чистый CSS, логин не нужен: копия `app.css`+`/tmp/vis/index.html` по HTTP (file:// в Playwright блокирован) → navigate/resize/screenshot + measure через getComputedStyle. Ловушка: png падает в корень+`.playwright-mcp/` — унести; зомби `node --test` валят Chromium.
- [[triplanio-ci-guard-is-code]] — TRIP-282: CI-гард = код, у него есть тест (`check-inline-styles.test.mjs` — ШАБЛОН: временный git-репо, гард подпроцессом, код выхода); зелёный тест ничего не значит, пока не увидел красным (мутация). TRIP-460: периметр — ОТДЕЛЬНАЯ проверка («всю ли зону видит», не «работает ли») и пинится тестом; одного теста на свойство мало — мутация ранга каскада прошла зелёной, доказывая не то свойство. Десятки уроков про escape/ключ маркера ЦЕЛИКОМ у гардов 2l/2m/2o/2p/2q.
- [[feedback-look-at-the-whole-section-after-editing]] — TRIP-282: после правки смотреть СЕКЦИЮ целиком, не свой дифф; ряд объявлен трижды на `ScreenAccount.jsx`. Правило: сложи отступы руками (`.card`+padding), «класс тот же» ≠ «выглядит так же», не копируй инлайны соседа (`button.acct-divrow`+brand-дефолт).
- [[feedback-unify-behavior-across-identical-elements]] — TRIP-140/293: меняешь поведение повторяющегося элемента → ВСЕ экземпляры через ОДНУ реализацию; проп-колбэк = узаконенная точка расхождения. Лечение: все ручки в одном объекте (`useForkList.js`+`forkFilter.js`); у поведения нет скриншота, гейт = тест на чистой функции.
- [[triplanio-button-loading-primitives]] — TRIP-130: единый in-flight — `<Btn loading>` (спиннер `.btn .spin`), `<Toggle busy>` (не оптимистичны), async-confirm `confirm({onConfirm:async})`. Реюзать это, не плодить ручные спиннеры.
- [[feedback-new-commits-need-new-pr-after-merge]] — после squash-мерджа пуш в ту же ветку повисает без PR. ПРЕ-PUSH ГЕЙТ отдельным шагом ДО пуша: `gh pr list --head <branch> --state all` → если MERGED, новая ветка от свежего origin/dev + cherry-pick + новый `gh pr create`.
- [[feedback-merged-pr-is-not-code-in-dev]] — TRIP-340→362: цепочка со ступенчатой базой (PR2→ветка PR1→dev) теряет поставку МОЛЧА, все индикаторы зелёные и не врут. Проверка: `git ls-tree -r origin/dev --name-only | grep <файл>` по добавленному файлу; мёржить цепочку СНИЗУ ВВЕРХ. Тело PR не репозиторий.
- [[feedback-start-every-task-from-fresh-dev]] — первый шаг любой задачи = `git fetch origin dev`, новая ветка только от свежего origin/dev; параллельные мержи ломают нумерацию миграций (таймстамп-коллизии).
- [[feedback-all-pushes-visible-linear-review]] — TRIP-100: после КАЖДОГО git push (вкл. обновление PR) обязателен явный отчёт в треде — номер+ссылка PR, HEAD-SHA, что изменилось; голый push без видимого в Linear PR-следа = провал.
- [[triplanio-cors-ratelimit]] — TRIP-67: CORS `*`→allow-list (единый `corsFor(req)` в `_shared/cors.ts`, ~45 функций); единый rate-limit примитив `rate_limit_hits`+RPC на 3 корзины (IP-лимит ДО проверки существования); rate_limited = статус 200.
- [[triplanio-cicd-github-actions]] — TRIP-73: авто-деплой edge-функций dev+prod, config-driven (verify_jwt только из config.toml, 13 pinned-false), миграции тоже через CI/CD (job migrate=db push); typecheck озеленён (checkJs:false), храповик `// @ts-check`. Грабли: `npm test` требует Node≥21.
- [[triplanio-cyrus-skills-loading]] — TRIP-23: self-hosted Cyrus без дашборда, версионируемый канал = репо `.claude/`; `.claude/skills/` грузится из ветки активного клона `main` (не dev/ворктри) → новый скилл после мерджа в main. ECC/superpowers не грузятся, аналог = свои SKILL.md.
- [[triplanio-mobile-adaptive-trip17]] — TRIP-17 (PR #114): confirm-диалоги на мобиле через `<Sheet>` (единая точка ConfirmDialog); flow создания на фикс-шелле `--vvh` (мёртвый `--kb-inset` удалён); новые классы `.ev-actions-top`/`.addon-card__status`.
- [[triplanio-dev-main-sync-no-squash]] — TRIP-18: squash-мерж плодит расхождение веток по SHA (ahead/behind); фикс — унифицирующий merge-коммит из обеих верхушек + FF-push в main и dev; branch protection в репо нет.
- [[triplanio-agent-connectors]] — TRIP-11: коннекторы агента — Supabase=MCP (токен не виден), Vercel=CLI (v54.16.0, avel123111, MCP нет), Linear=MCP (team UUID 6035454e…, префикс TRIP), Notion=REST через curl (NOTION_TOKEN); python3 3.11.15+uv ЕСТЬ.
- [[triplanio-telegram-unlink-seam]] — TRIP-415: отвязка Telegram в ОДНОМ шве `disconnectTripTelegram` (delete + external-only emit `trip_telegram_unlinked` по каждому снятому chat_id → n8n); срабатывает из всех бизнес-точек. Фикс: выключение аддона `telegram_assistant` теперь рвёт привязку (afterWrite `trip-settings/settings`, инвариант «аддон off ⇒ привязок нет»), раньше бот слал напоминания дальше. Ловушка: destroy-кейсы (удаление трипа=FK CASCADE, аккаунта=атомарный SQL) НЕ через шов и НЕ эмитят намеренно.
- [[triplanio-pro-rollback-addons]] — миграция 0061: аддоны были «липкие» (Pro бесплатно после отмены/возврата); SQL revoke_*_pro_addons (гейт NOT is_trip_pro)+единый `_shared/telegramTeardown.ts`+оркестратор из webhook/reconcile.
- [[triplanio-mobile-dnd-press-feedback]] — press-фидбэк DnD городов, общий useRouteDnD+CityRow на 3 экранах; depress при касании+lift при арминге (430→300мс), класс `.te-row.is-pressing`; десктоп не тронут.
- [[triplanio-auth-isloadingauth-remount-trap]] — `isLoadingAuth` ремаунтил всё app (беск. перезагрузка при Stripe-возврате); фикс — checkUserAuth стал silent (loadUserProfile не флипает isLoadingAuth), фоновые рефреши держать silent.
- [[triplanio-account-deletion-design]] — TRIP-78: удаление аккаунта = soft-delete/анонимизация (обезличить users email/full_name/avatar→null+deleted_at, удалить auth.users, хранить контент+Stripe); код НЕ начат.
- [[triplanio-tripmembers-name-cache-smell]] — TODO тех-долг: `trip_members.user_full_name/invite_email` = денормализованный дубль users (рассинхрон+PII-утечка), оставить только для приглашённых без user_id; отдельный тикет.
- [[triplanio-editing-by-deadcode-drop]] — TODO тех-долг: `editing_by/editing_since`+5 RPC мертвы (live-редактор TRIP-126), дропнуть отдельным тикетом, НЕ часть TRIP-78.
- [[triplanio-security-audit-2026-06-21]] — live-подтверждён: P0 viewer-write на 9 таблицах (RLS=is_trip_participant без роли, `_can_edit_trip` не используется); P1 syncTripExpense без auth, callTriplanioAi/planTripWithAi без Pro-гейта, висячие DML-гранты; код НЕ менялся.
- [[feedback-redesign-means-from-scratch]] — РЕДИЗАЙН = с нуля, не рескин; защищены ТОЛЬКО карточки эвентов, остальное (вкл. empty-day) рисовать структурно-разным; погоды и cityhero НЕТ.
- [[feedback-ask-before-removing-feature-intent]] — удаления, меняющие СМЫСЛ фичи, — флагнуть и спросить ДО правки; явный мусор чистить сразу; сомнение считать продуктовым.
- [[feedback-do-cleanups-immediately]] — найденный в задаче мёртвый код/осиротевшие i18n/мелкий рефактор доводить в том же ответе; отдельно флагить только смену поведения.
- [[triplanio-trips-dates-dropped]] — миграция 0052: `trips.start_date/end_date` ДРОПНУТЫ (100% NULL, никто не писал); убраны фоллбэки в `_trip_anchor_date`/add_city/copyTrip/FE; ≠ city_visits-даты (те живые).
- [[triplanio-revenuecat-assessment]] — ADR Proposed: RC = слой энтайтлментов ПОВЕРХ провайдеров; web-only переезд не оправдан, оправдан мобайлом (принять RC сейчас); pro_trip per-trip RC не представляет → свой ledger остаётся; SoT=провайдер, recompute-кэш остаётся.
- [[triplanio-recompute-anchor-dates]] — миграция 0049: `recompute_trip` не писал start/end узлов start/end → хранилась add_city-затравка, валидатор врал ложным TR_DEP_DAY; фикс: start=день вылета, end=чекаут+day_change; зеркало FE layoutDates.
- [[triplanio-standalone-screens-unification]] — глобальный CreateTripProvider (FAB=+шит на месте, Pro-лимит=серверный getActiveTrips); Главная/Статистика/Нотиф full-width; `.tr--add` плейсхолдер; Layout.jsx мёртвый.
- [[triplanio-public-trip-redesign]] — анализ: экран `PublicTrip.jsx` (/public/trip/:id?t=, не Lumo), токен/ссылка развязаны (не трогать); ReadOnlyTimelineView дублирует buildEventStream (3-ю ленту нельзя, rule #6); риск `select('*')` в getPublicTrip.
- [[triplanio-trip-limit-sources]] — фикс: create_trip считал лимит по мёртвой `trips.end_date` (всегда активно) → ложный TRIP_LIMIT_REACHED при 0 трипов в UI; миграция 0040 считать по city_visits; 4 места дублируют логику (долг).
- [[triplanio-stats-dashboard-redesign]] — TRIP-270 (PR #614): плитки переездов /stats — RPC `get_user_travel_stats` возит переезды НАБОРОМ строк `[{transport_type,start_date}]`; наземные = total-flights вычитанием; предикат `=== 'plane'`. `transport.js` в `travel-stats.js` НЕ импортировать (lucide уронит `node --test`).
- [[triplanio-viator-cities-integration]] — cities построен (≈4894), edge stay22Accommodations+viatorActivities ACTIVE; радиус `resolve_city_id` оставлен 30. ⚠️Обоснование историческое: TRIP-146 дропнул триггер `set_city_id` — сегодня у city_visits НОЛЬ пользовательских триггеров, manual-города из рантайма не создаются.
- [[triplanio-create-flow-redesign]] — оба флоу (manual+AI = ManualPlanner), 2 PR; PR1 сделан (общий useRouteDnD, TSE behaviour-preserving), PR2 не начат (контейнеры/sticky-футер back/reset/next/мобайл карта-сверху).
- [[triplanio-noheader-layout-implemented]] — убран TripScreenBar на всех экранах (синий AppHeader оставлен); Бюджет→`.bgt-head`+мобильный `.bgt-fab`; планировщик→шапка «Маршрут» `.ts-routehead`+степпер+Radix StartCalendar.
- [[triplanio-noheader-layout-prototype]] — макет всех 10 экранов трипа без шапок (Lumo light, нав левым меню/bottom-nav+«Ещё»), файл TRIP_SCREENS_NOHEADER_LAYOUT_LUMO_2026-06-17.html; в код не перенесено.
- [[triplanio-map-markers-ring]] — маркеры/линии в стиль Ring Lumo на токенах (день/ночь без перерисовки), selected провязано (ScreenMap=activeIdx, редактор=leftPanel); линии из `--map-route`; выпилены MARKER_*/ROUTE_COLOR.
- [[triplanio-booked-flag-plan-fact]] — анализ (код НЕ начат): is_booked на hotel/transfer/activity, booked=абсолют.даты, unbooked=выведено из окна города; сдвиг делать на сервере; P1 план-vs-факт через group_id (заложить заранее).
- [[triplanio-pro-visual-qa]] — 16 точек входа Pro-визуала (EP-01…16), правило офферов 2-vs-3, гейты функций (canEditMode/tripIsPro); правила→Notion, чеклист→xlsx; баг-риски бейджа карточки.
- [[triplanio-city-visits-country-dropped]] — TRIP-223 (PR #479): `city_visits.country` ДРОПНУТА, страна из `country_code` через `Intl.DisplayNames` (`fmtCountry`/`localizeCountry`) в точках показа → ре-локализация под язык зрителя. ⚠конвенция: `trip-cities.js` держим чистым (без i18n/luxon) ради `node --test`.
- [[triplanio-geocoding-v2-design]] — TRIP-146: идентичность города → локальный GeoNames-газеттир (ключ `geonameid`), cities как директория, единственная точка поиска RPC `search_gazetteer`; cold/hot split (все локали сыро / снимок en/es/ru); bulk через чанк-INSERT не COPY.
- [[triplanio-geocode-cache]] — TRIP-145 P1: `geocode_cache` в Postgres, geoLocationiq читает/пишет (hit не ходит наружу, 429/502 не кэшируется); LocationIQ = итоговый геокодер; направление P2 token-bucket, P3 единый resolveCities.
- [[triplanio-ai-city-resolve-directory-en]] — резолв EN-имени по cities ДО LocationIQ (RPC resolve_cities_local)+EN-фолбэк+learn_city (self-heal); Sentry «geocode batch unresolved»=throttle 2 req/s, НЕ «имя не найдено»; корень rate-limit открыт.
- [[triplanio-city-country-count-unification]] — единый источник `src/lib/trip-cities.js` (isTransitVisit/uniqueCityCount/uniqueCountryCount), только kind==='transit'+дедуп везде; фикс расхождения цифр между экранами.
- [[triplanio-overview-redesign-todo]] — макет Обзора (стат-полоса full-width вверх, A/B To-Do) + виджет недостающих броней (город без hotel_stay/разрыв без transfer); is_booked в модели НЕТ; в код не перенесено.
- [[triplanio-fork-panel-hotels-lumo]] — макет fork-панели «Отели» в Lumo (manual-CTA+партнёрки состояния 0-4+список Stay22+loading/empty/error+mobile sheet); в коде partner=Airbnb; в код не перенесено.
- [[triplanio-stay22-hotel-fork]] — отели Stay22 в fork-панели: у API ТРИ гео-режима и это РАЗНЫЕ выборки. `address` выпилен (резолвит имя в центр МУНИЦИПАЛИТЕТА: LA 26 км, Токио 132) — он и давал «окраины»; ТОЧКА = ~140 ближайших в пределах радиуса (в плотном городе это кварталы), КОРОБКА = разброс по площади (на LA пересечение с точкой НУЛЕВОЕ). Пул склеен из обоих (`GEO_MODES`, ±12 км, 2 раунда × 100, потолок 300), «рекомендуем» = расстояние. Прошедшие даты запроса не порождают (были 400→502). Ловушки: ToS запрещает хранить листинги (серверный кэш закрыт), `provider` принимает одно значение, координаты `city_visits` разъехались с газеттиром на 48% посещений.
- [[triplanio-bottomnav-header-unification]] — макет: 4 варианта боттом-нава (контекстный: трип=лензы/список=глоб) + схлоп 3 хедеров трипа в 1; в код не перенесено.
- [[triplanio-security-tiers-architecture]] — TRIP-124/190/120: ★★ярусная модель БД A(контент)/B(авторитетное)/C(личное)/D(справочники) + secdef-функции, манифест-SoT `scripts/ci/security-tiers.mjs`, две двери; CI-страж `check-security-tiers.mjs` STATIC(2e)+LIVE. Менять правило = 1 предикат+грант+строка манифеста.
- [[triplanio-viewer-write-rls-escalation]] — TRIP-124 (было 136/137): ЗАКРЫТО — 9 контент-таблиц → роль-осведомлённый RLS (`WRITE=_can_edit_trip`), anon DML снят, `/edit` под FE-гардом. Историч: viewer писал брони/city_visits в обход `_can_edit_trip`.
- [[triplanio-live-edit-server-recompute]] — TRIP-126: уход от песочницы/лока/драфта к live-записи + серверный recompute (как Stippl); лок = колонки на trips; эпик Ф1–Ф7; Ф0 завершена (R1/R2/R3), код НЕ начат.
- [[triplanio-ecc-toolkit]] — TRIP-23: карта ecc:* скиллов по умолчанию для triplanio (DAILY/LIBRARY/SKIP списки), как ставить (репо-install через /ecc:configure-ecc, не сырой node).
- [[triplanio-timeline-account-edit-batch]] — батч UI: таймлайн day-separator (город под днём недели, подсветка today+автоскролл, рейл без waypoint); EventAiBlock доводка; Account inbox-карточка→/inbox с непрочитанными.
- [[triplanio-ai-pattern-eventaiblock]] — EventAiBlock → канон-паттерн A4 (`.ai-blk` единый источник, мёртвый `.aiblk` удалён), AI-кнопки через `<Btn variant="ai">`. TRIP-295 токен-слой AI без алиасов; грабля: `.btn` держит `background-origin: border-box`, НЕ снимать.
- [[triplanio-overlay-pro-unification]] — аудит всех оверлеев+Pro-слоя → канон Lumo; Pro ремап warm→золото `--pro`; bottom-sheet `.sheet<640px`; инлайн-ошибки `.field--error`/`.err`/`.wrn`; тосты 4 типа; план Ф0–Ф7.
- [[triplanio-map-singleton]] — один mapboxgl.Map на сессию, reparent между экранами (MapProvider над роутером); MapView+FlowMap делят инстанс; границы: не переживает reload/др.юзера.
- [[triplanio-overview-screen]] — новый ГЛАВНЫЙ экран трипа (lens overview, дефолт вместо timeline): карта+5 стат/бюджет+участники; цвета категорий→Lumo `--cat-*` (categoryStyles мёртв).
- [[triplanio-budget-docs-screens-lumo]] — макеты BudgetLens+DocsLens в Lumo (донат/сегмент кат⇄город/planned-бары/Pro-гейт+карточки доков с тип-чипами/dropzone); 3 файла в «design new», в код не перенесено.
- [[triplanio-chat-notifications-lumo]] — макеты Чат (пузыри me/участник/AI, @Triplanio, thinking) + Уведомления (поповер колокольчика+Входящие группы дат, invite-действия); в код не перенесено.
- [[triplanio-account-screen-lumo]] — макет Аккаунта (4 состояния подписки+TG); каналы напоминаний=расширяемая секция (1 TG↔много трипов), языки=поповер с поиском.
- [[triplanio-repo-location]] — папка `triplanio_new` = source-of-truth клон GitHub (инструкция «игнорировать» устарела); App 2 = прототип дизайна.
- [[triplanio-horizon-design-system]] — НОВЫЙ стандарт Horizon взамен Atlantic (Unbounded+Onest, азур primary, типы событий/AI-градиент/Pro-золото, light+dark, AA); в код не перенесён.
- [[triplanio-ui-audit]] — отчёт UI_AUDIT_2026-06-05, 2 корня (типошкала+2 движка), P0 сделано/build зелёный, P0-6 Members link ждёт решения, P1/P2 дорожная карта.
- [[triplanio-login-css-token-leak]] — ИСПРАВЛЕНО: login.css глоб. `:root` перекрывал `--ink` (#0F172A) на всех роутах после входа → тёмный текст на тёмном в dark; фикс `:root`→`.auth`.
- [[triplanio-style-token-audit]] — STYLE_AUDIT_2026-06-06: ВЫПОЛНЕНО до Lumo (единый источник цвета, типографика 100% под Lumo), гард `check-design-tokens.mjs` (типографика блокирует, цвет репорт); остаток цвета ждёт Lumo.
- [[triplanio-lumo-gap]] — TRIP-53 аудит: зонтичная задача Lumo фактически закрыта (редактор/уведомления/бюджет/документы перенесены, погода намеренно убрана); остаток = дубль цвет-токенов в login.css.
- [[triplanio-design-ux-audit]] — слой ВЫШЕ кода: сломан голос (ты/вы, битые строки, $-цены), consolidation, видение «editorial concierge»; DESIGN_UX_AUDIT_2026-06-05; ждёт живого прохода.
- [[triplanio-copy-audit-2026-06-06]] — сплошной copy-аудит 1921×3 ключа: 192 механ. правки применены, 12 спорных переименований ждут Pavel, баги текст↔функция; COPY_STYLE_GUIDE+Notion.
- [[triplanio-vercel-hobby-blocks-collaborator-commits]] — деплои BLOCKED, когда автор коммита ≠ владелец (коммиты Ильи с локальным email); фикс=Redeploy владельцем или PR-мерж Pavel; навсегда=Vercel Pro.
- [[triplanio-vercel-urls]] — дев-алиас triplanioapp-git-dev-…vercel.app, main-алиас + triplanio.com; защищённые роуты требуют логина (для браузерного репро нужна залогиненная вкладка).
- [[triplanio-deploy-topology]] — TRIP-134 Ф3: ОДИН воркфлоу `supabase-deploy.yml` катит бек И фронт цепочкой gate→migrate→deploy→vercel→notify (фронт не раньше бека), фронт через Vercel-токен; PR-гарды в checks.yml (2a/2b/2c/2d); агент НЕ деплоит сам.
- [[triplanio-payments-foundation-rebuild]] — TRIP-32: чистый платёжный фундамент по `payments-architecture.md` (раздельные purchase/subscription, каталог в БД, webhook_event, outbound_idempotency, адаптер Stripe-only), кэш права оставляем; strangler фазами.
- [[triplanio-payments-audit-2026-06-29]] — TRIP-32: ядро чистое после переписывания 2026-06-28; открытые вопросы (planTripWithAi без Pro-гейта=решение Pavel, нет reconcile-on-read для pro_trip); код НЕ менялся.
- [[triplanio-payments-deep-audit]] — P0: authenticated пишет колонки энтайтлмента (users.subscription_*/trips.is_pro_trip) через PostgREST → бесплатный Pro (prod+dev); ядро солидное, не переписывать; +P1 parseBookingWithAi без гейта, рефанд подписки не снимает Pro.
- [[triplanio-payments-phase-status]] — Ф1+Ф2+Ф3 done+задеплоено (каталог product-id, миграция 0050, webhook v30 single-writer, recompute-on-read=reconcileEntitlement); осталось сверить, что endpoint в Stripe Dashboard подписан на события.
- [[triplanio-backend-migration-notes]] — TRIP-102 накопительная: Supabase-специфика, не переносимая в лоб (storage.buckets/objects+политики, `auth.uid()` в RLS, storage.protect_delete, pg_net/supabase_vault); зеркало в Notion.
- [[triplanio-migration-naming-drift]] — TRIP-68/102: история схлопнута в единый `20260625120000_baseline`, репо↔dev↔prod журналы идентичны (1 запись), Git-интеграция не падает; миграции через CI/CD (job migrate, db push); новые ТОЛЬКО `supabase migration new` (таймстамп).
- [[triplanio-bulk-data-migrations-constraints]] — TRIP-145: db push НЕ умеет COPY-from-stdin (→батчевые INSERT); GitHub не диффит >40МБ; УПАВШАЯ миграция журналируется → лечить новым таймстампом; тяжёлый UPDATE → statement_timeout 600s+temp/set-based; сверять реальное состояние БД read-only.
- [[triplanio-ai-usage-logging]] — таблицы `ai_model_prices`/`ai_usage_events`+БД-триггер cost-снапшот+поллер n8n AI Usage Logger; прайс-бук Gemini/Mistral; осталось креды+атрибуция.
- [[triplanio-prod-maps-broken-getmapsapikey]] — РЕШЕНО: getMapsApiKey выпилен везде, карта на Mapbox `VITE_MAPBOX_TOKEN` (build-time, уже на main); ждёт push→Vercel.
- [[triplanio-prod-editmode-missing]] — РЕШЕНО: лок-RPC/миграции (trip_edit_lock, edit_mode_rpcs, save_trip_edit, city_visits_*) ЕСТЬ на prod Supabase; режим редактирования рабочий со стороны БД.
- [[triplanio-i18n-tolgee-incontext]] — TRIP-129 Config X (PR #298, не мёржён): Tolgee SDK за фасадом i18n (1837 call-sites `t()` не тронуты), in-context через расширение; в бандле только ядро+FormatSimple+BrowserExtensionPlugin; свой словарь=авторитет, Tolgee=оверлей. CI-синк переделан 08.2026: отдельный воркфлоу `i18n-sync.yml` (крон+кнопка, живёт на `main`, цель `dev`), переводы едут PR-ом, не прямым пушем. Ловушка: прямой пуш в `dev` невозможен ПО ПОСТРОЕНИЮ (required-чек рождает только `pull_request`), а `GITHUB_TOKEN` не триггерит workflow → нужен PAT `I18N_SYNC_TOKEN`.
- [[triplanio-localization]] — глоссарий (ты/путешествие/событие/раздел), объём, i18n vs вендор, in-app админка, файл ревью Шага 0.
- [[triplanio-es-titlecase-debt]] — TODO: испанские локали повсеместно в Title Case, нужен sentence-case проход по всем es/*.js (с белым списком проперов); отдельно от RU-глоссария.
- [[feedback-no-manual-deploy-cicd-only]] — агент НЕ деплоит сам (ни MCP/CLI/Management API) и не предлагает ручной деплой; всё через PR→мердж→GitHub Actions; новые миграции только `supabase migration new` (таймстамп), не `00NN`; ручной = аварийный фолбэк только Pavel.
- [[feedback-reuse-first-unification]] — перед «готово» по UI обязателен reuse-аудит: вязаться к существующим компонентам/токенам/брейкпоинтам (≈880/760/640), не плодить дубли; новое только с обоснованием; закрытие строкой «Reuse audit». Hard rule #6.
- [[feedback-flag-heavy-capabilities]] — нельзя включать в скоуп/ТЗ требования на возможности, которых нет (рефанды), даже если пункт был в чужом тикете; давать минимальное решение без новой capability.
- [[feedback-design-for-scale-not-now]] — не говорить «готово» без проверки конкурентности/общих лимитов/нагрузки; клиентский ретрай = thundering herd; затычку называть затычкой и сразу давать серверное решение (кэш/троттл/очередь).
- [[feedback_base44_analysis_rule]] — КРИТИЧЕСКОЕ: ВСЕГДА base44 коннектор + репо + схема Supabase ДО любых изменений. Без исключений.
- [[triplanio-migration-plan]] — 6 фаз миграции, домен triplanio.com/trips, wiring-стратегия, статус 2026-05-26.
- [[triplanio-code-analysis-rule]] — перед изменением в triplanio_new: full dep-chain из base44 → только потом код.
- [[triplanio-status]] — актуальный статус всех фаз миграции, таблица функций, что нужно от Pavel.
- [[triplanio-done-criteria]] — готово = визуал нового дизайна + полная логика base44 (обе оси).
- [[triplanio-userid-migration]] — рефакторинг идентификатора пользователя email→uuid; решения + ТЗ-файлы в репо.
- [[triplanio-n8n-jwt-auth]] — исходящие вызовы в n8n требуют HS256-JWT (`signN8nJwt`), не сырой N8N_SECRET.
- [[triplanio-stripe-integration]] — состояние, 2 Supabase-проекта (prod/dev), webhook-URL, целевая архитектура «1 режим на проект».
- [[triplanio-validation-unification]] — единый validateEntity + единый показ (инлайн+панель) + блок всех save-дыр; решения Pavel (out-of-bounds=error, все даты обязательны/предзаполнены, фронт-only); фазы Ф0–Ф5.
- [[triplanio-pro-model]] — ★ИСТОЧНИК ИСТИНЫ тарифов Pro; якорь TRIP-181 сформулирован в долларах ($39.99/год·$6.99/мес·$8.99/трип), а прод СПИСЫВАЕТ ТЕ ЖЕ ЧИСЛА В EUR (каталог `provider_price` live, сверено 2026-08-31) → конкурентную рамку считать по курсу, не читать числа как долларовые; граница Free/Pro (⚠️«бюджет полностью Pro» НЕВЕРНО, проверено по коду 04.09.2026: виджет бюджета на обзоре отдаётся без `isPro` — `OverviewLens` его даже не принимает; гейт живёт только в `BudgetLens` и закрывает ручное управление и правку курсов), value metric не per-seat; флаги: WTP RU не валидирован, Stripe не сеттлит RUB.
- [[triplanio-pro-audit]] — уязвимости enforcement Pro (V1–V8), расхождения визуала; отчёт PRO_AUDIT_2026-05-29.md.
- [[triplanio-timeline-order]] — фикс варнингов «Нет переезда»: рендер Hero по дню прибытия, sortVisits тай-брейк по end, единый prevCity.
- [[triplanio-ai-booking-parse]] — EventAiBlock с base44 InvokeLLM на свой pipeline: edge `parseBookingWithAi`→n8n→Gemini; промпты в n8n.
- [[triplanio-gettripbyid-idor]] — IDOR: getTripById/getTripByTelegramChatId (verify_jwt=false + ноль проверки = чтение любого трипа по UUID); фикс Bearer N8N_SECRET, НЕ исправлено.
- [[triplanio-gettripdetails-verifyjwt]] — дыра getTripDetails (fail-open auth + verify_jwt=false + публичный anon-ключ = чтение любого трипа по id); фикс fail-closed внесён, ждёт деплоя.
- [[triplanio-telegram-bot]] — модель chat_id→трипы, баг dev/prod URL, фикс эндпоинта на массив trips, промпт v2 в Notion.
- [[triplanio-telegram-multilink]] — many-to-many trip↔chat_id, n8n=владелец webhook (вариант A), split binding, судьба функций; ждёт 2 решения Pavel.
- [[triplanio-i18n-no-hardcode]] — не хардкодить UI-строки: фронт через `t()` (en/es/ru), бот=message из edge-функции; SettingsLens захардкожен RU=долг.
- [[triplanio-inapp-chat-sync-idea]] — отложено; анализ async↔sync, рекомендация Путь 1.
- [[triplanio-deploy-verify-jwt]] — грабли: CLI сбрасывает verify_jwt в true и ломает N8N_SECRET/webhook-функции; всегда `--no-verify-jwt`.
- [[triplanio-frontend-repo]] — `triplanio_new` = живой репо; ChatLens/ChatWidget реальные, redesign/* мокапы; баг бюджета price 0 (триггер).
- [[triplanio-chat-caret-drift]] — РЕШЕНО: каретка уезжала вперёд текста ≤640px — глоб. `@media(max-width:640px){.textarea{font-size:16px}}` (анти-iOS-зум) бил textarea, но не div-оверлей `.chat-ov` (16 vs 14px); фикс — оба слоя 16px на мобиле.
- [[triplanio-trippayload-no-budget]] — getTripById/getTripByTelegramChatId больше не отдают budget (задеплоено prod+dev); промпты ботов/n8n с budget* надо обновить.
- [[triplanio-tg-connected-accounts]] — секция «Подключённые аккаунты» + edge telegramGetMyIntegrations + общий TelegramUnlinkDialog (заменил window.confirm); выпил легаси TripSettings; задеплоено.
- [[triplanio-dev-parallel-env]] — dev=dev-Supabase+sandbox-Stripe; выравнивание (getPublicTrip verify_jwt, 4 функции, Stripe-секреты канон. имена); `scripts/clone-trip.mjs` (prod→dev, ремап на dev-юзера).
- [[triplanio-members-roles]] — нет роли editor; статусы (active без текста, declined «Отклонил»); переприглашение declined→pending; removeTripMember FK-фикс (миграция 0011 CASCADE); шеринг в сайдбаре; edgeErrorMessage.
- [[triplanio-copy-trip]] — TRIP-272 (PR #635): вход ОДИН — «Копия путешествия» в SettingsLens, порядок клик→confirm→гейт лимита→копирование в шве `startCopy` (CreateTripProvider); лимит добивает триггер `trips_enforce_limit`; Pro не наследуется. ⚠️клипборд-ключи `common.copy` вешать нельзя (ложный реюз).
- [[triplanio-services-widget]] — right-rail kind-based (НЕТ колонки status), плейсхолдеры esim/car_rental + «Ещё», фикс плюса над текстом.
- [[triplanio-trip-settings-display]] — display-bag (booking_warnings, chat_widget, default ON), виджет чата гейт (аддон chat+флаг), карточки настроек conditional по аддонам, `safeStorageName` единый санитайзер загрузок.
- [[triplanio-edit-mode]] — Edit Mode песочница (отдельный экран, ничего не пишется пока редактируешь, гейт сохранения при варнингах). ⚠️ЗАМЕНЕНО [[triplanio-sandbox-redesign]] 2026-06-03.
- [[triplanio-sandbox-redesign]] — ФИНАЛ 2026-06-03: две зоны (Маршрут=геро по cityVisit / Лента=хронология), геро вне ленты, переезд со снапшот-городами (не FK), валидации→soft, CASCADE→SET NULL.
- [[triplanio-trip-model-redesign]] — ⚠️ОТКЛОНЁН 2026-06-01, заменён [[triplanio-edit-mode]]; история анализа (развязка/city_ref/position/время-tz), НЕ план.
- [[triplanio-timezone-dedup]] — TODO выпил Google Time Zone (дубль с Open-Meteo geo.js getTimezone); но Open-Meteo тоже коммерч.-лимит → лучше offline tz-lookup/geo-tz.
- [[triplanio-geo-open-questions]] — гео-стек: карта→Mapbox сделано; поиск→Geoapify (план), маршруты→открыто, таймзоны→tz-lookup (решено, не сделано); +оптимизация смены темы.
- [[triplanio-mapbox-migration]] — РЕШЕНО Вариант 1: карта+поиск+маршруты на Mapbox, Permanent Geocoding для хранимых коорд, tz-lookup офлайн, кеш маршрутов; план Ф0–Ф5; ОТКРЫТО ToS distribution.
- [[triplanio-event-edit-redesign]] — полный порт EventEditDialog на ДС (тройная сверка паритета) + трансферы с пересадками (UI=мокап, бэк=§11 waypoint-цепочка); решения Pavel 2026-06-02.
- [[triplanio-planner-redesign]] — единый create-flow (manual+AI); решения Pavel: убрать transport-шаг, сохранить AI-активности, без броней в создании, без bookings-входа.
- [[triplanio-entitlement-reconciliation-todo]] — TODO: вебхук + периодический re-fetch authoritative-API (на старте/по cron) ловит пропущенные вебхуки; один механизм на Stripe-web и будущий IAP; истина=Supabase.
- [[triplanio-native-iap]] — стек натива=Flutter; Pro=цифр.подписка → Apple IAP/Play Billing через RevenueCat (агрегатор чеков), Supabase=истина по доступу; фазы Ф0–Ф4.
- [[triplanio-trip-delete-fk]] — ★ДЕНЬГИ: FK удаления трипа — notifications=CASCADE(0020), partner_clicks+trip_subscriptions=SET NULL(0021), prod+dev выровнены; «блок пока активна» не решение.
- [[triplanio-timeline-city-overlap-bugs]] — отель привязан date-range эвристикой (не city_visit_id), дедлайн вне окна дат dropнут, двухфазный рендер дня ставит чекаут ниже перелёта; фиксы + сверка с base44 PDF.
- [[triplanio-free-services-risk]] — OSRM/Nominatim/OSM-тайлы/Open-Meteo: у всех ToS/нагрузочный подвох в проде; прод-путь по каждому (self-host / платный / offline).
- [[triplanio-editor-panels-redesign]] — модалки event view/edit → левые in-place панели; решения Pavel (брони в БД сразу, переезды FK+«вне плана», варнинги инлайн); одна логика/две оболочки.
- [[triplanio-editor-redesign-mockup]] — макет «editorial concierge» (Fraunces, stat-band, app-shell хедер+скрытое меню+lens, DnD, живая карта); в код не перенесено.
- [[triplanio-editor-ui-redesign]] — сетка без обводки+сепараторы, seam-чипы переездов, FLIP-DnD, collapsed-сайдбар, FAB варнингов, оптимистичное создание броней + превью маршрута.
- [[triplanio-tmp-transfer-uuid-bug]] — создание переезда к несохранённому (tmp-) городу падает на uuid; стоит guard+месседж, нужен правильный фикс через draft/transfers_new.
- [[triplanio-overnight-transfer-flag]] — day_change на transfer → +1 gap каскадом на след. города; ядро (recompute/gap) готово; решения Pavel (флаг на трансфере, datetime декуплен=варнинг, AI авто-подъём, default false).
- [[triplanio-pro-status-hook]] — плашка upgrade моргала на границе edit↔trip (локальный useState на отдельном роуте) → фикс кэш-хуком useTripProStatus; меню edit/трип = один TripSidebar.
- [[triplanio-reminder-tz-fix]] — get_pending_reminders слал со сдвигом на offset города (Мадрид в 23 вместо 21); фикс reminder_true_instant (настенное+city tz→реальный момент), prod+dev задеплоено.
- [[triplanio-sentry-monitoring]] — один EU-проект+env-тег, DSN, переменные; Ф1 фронт СДЕЛАН (errors-only, PII-скраб); Ф4 edge и Ф6 Notion — нет.
- [[triplanio-cancel-downgrade-no-tripsubrow-bug]] — БАГ dev: вебхук .deleted записан processed, но users остался pro; downgrade резолвит юзера ТОЛЬКО через `trip_subscriptions.stripe_subscription_id`, строки нет → no-op. Тот же риск в prod.
- [[triplanio-mcp-on-demand]] — MCP-серверы «по требованию»: n8n снят из `.mcp.json` (тяжёлые инструкции в каждое сообщение, нужен редко) — существует, включается копипастом блока обратно. Всегда-вкл: sentry/tolgee/posthog/playwright; платформенные (github/Supabase/Linear/Notion/Vercel) — вне репо.
