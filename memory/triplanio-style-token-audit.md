---
name: triplanio-style-token-audit
description: "Аудит привязки визуала к токенам Atlantic — где захардкожено, что решить перед рефактором"
metadata: 
  node_type: memory
  type: project
  originSessionId: bba198cd-d0b1-4a1f-a89f-4a8a6d2ede4b
---

Аудит 2026-06-06 (только отчёт, кода НЕ трогали). Цель Pavel: подвязать ВСЕ элементы визуала под дизайн-систему, унифицировать в рамках допустимых вариаций. Решено мерить по текущей **in-code Atlantic** (не Horizon), сначала отчёт → потом вместе определяем «все вариации».

Файл: `triplanio_new/STYLE_AUDIT_2026-06-06.md`.

Факты:
- Система ЕСТЬ и хорошая: `src/design/app.css` = 136 токенов (brand/neutrals/status/AI/ev-типы/radius/shadow/ease/fs-ладдер), light+dark. ~900 inline-style уже через `var(--token)`.
- Проблема = протечки+дубли, а не отсутствие системы.
- **3 слоя:** A) shadcn `ui/*`+index.css (HSL, чисто кроме toast); B) Atlantic `design/index.jsx`+`app.css` (hex, основной слой; `design/index.jsx` = ОБЩАЯ либа Avatar/Btn/Dialog, сама хардкодит hex); C) страницы/компоненты (микс, тут большинство нарушений).
- **V1 P0 root cause:** ДВА движка токенов — `#2167e2` задан и как HSL `--primary` (index.css) и как hex `--brand`/`--ev-hotel` (app.css); правки в 2 файлах, нет синхронизации.
- **V2 P0:** 187 сырых tailwind-палитра классов; ХЕДЛАЙН — `ReadOnlyTimelineView.jsx` красит типы событий мимо токенов (activity=violet вместо терракота `--ev-activity`, transfer=blue вместо teal). Одна концепция = разные цвета на разных экранах. Также `buildBookingPlatforms.jsx` хардкодит hover-состояния.
- **V3 P1:** 223 hex-литерала в JSX (26 файлов; топ LandingPage 32, design/index 18, ManualPlanner 12, SettingsLens 10, ValidationUI 9).
- **V4 P1:** 31 inline-style с литеральным цветом (скримы/градиенты rgba без токена).
- **V5 P1:** fs-ладдер есть, но app.css хардкодит 60 px, JSX `text-[11px]`×23 и т.п.; нет токена 9/10px.
- Пробелы токенов: НЕТ Pro/gold, нет scrim/overlay, avatar-ramp дублируется (надо промоутить в `--avatar-1..8`).

**Ф1 ФУНДАМЕНТ СДЕЛАН (2026-06-06, dev, build зелёный):** index.css :root = единый источник цвета (HSL, для alpha-модификаторов; используются 300+); app.css brand/ink/line → алиасы `hsl(var(--primary/foreground/border))` (сдвиг ≤5/255, незаметно); добавлены токены `--pro/--pro-soft(+12)`, `--scrim`+`--overlay-grad`, лесенка `--avatar-1..8`, `--fs-nano:10px` (light+dark); выпилен МЁРТВЫЙ `data-vibe` (editorial/tech нигде не выставлялся) + неиспользуемые шрифты Instrument Serif/Space Grotesk. Долг: Inter грузится 4× (app.css/index.css/login.css/index.html) — консолидировать.
**ДИАЛОГИ фрагментированы (Pavel попросил унифицировать):** сосуществуют shadcn ui/dialog + ui/alert-dialog (Radix) + кастомный Atlantic Dialog (design/index.jsx:328) + руками собранный ShareDialog (inline position:fixed+rgba) + бошпоке (ForkPartnerModal/TripLimitDialog/ServiceDialog/TripFormDialog/EventModal/Telegram/ProLocked/TripProInfo/Payment*). Живые нативные window.confirm в 5 местах: MembersLens:285, SettingsLens:517/538/539, DocsLens:222 (хотя ConfirmDialog есть).
**РЕШЕНО Pavel 2026-06-06:** (1) канон диалога = **Radix shadcn** (ui/dialog контент + ui/alert-dialog подтверждения); кастомный Atlantic Dialog, ShareDialog, бошпоке и 5 window.confirm мигрируем на него. ConfirmDialog.jsx УЖЕ на ui/alert-dialog+токенах — это готовый примитив подтверждений. (2) канон красного = **Atlantic --danger #c4364c**. СДЕЛАНО: index.css --destructive→351 57% 49% (light)/351 71% 65% (dark)=#c4364c/#e5687a; app.css --danger и --ev-deadline→`hsl(var(--destructive))` (один красный на оба движка), build/PostCSS зелёный. (3) secondary/accent — не трогаем (shadcn-only).
**Ф2-ПОДТВЕРЖДЕНИЯ СДЕЛАНО (2026-06-06, dev):** новый `src/components/common/ConfirmProvider.jsx` = промис-`useConfirm()` поверх ConfirmDialog (Radix), замонтирован в App.jsx внутри QueryClientProvider (i18n выше — ок); 5 нативных window.confirm переведены на `await confirm({title, variant:'destructive'})` (MembersLens.removeMember, SettingsLens.leaveTrip/deleteTrip×2, DocsLens.handleDelete). window.confirm в src больше НЕТ. ESLint 0 ошибок, Babel-parse ок. ⚠️esbuild в песочнице = macOS-арх (репо с мака), не запускается; vite build очень медленный — верификация через eslint+babel-parser. ОСТАЛОСЬ в Ф2: ShareDialog (руками position:fixed+rgba) + бошпоке-модалки → ui/dialog; нативные alert() (инфо-ошибки) → toast/ConfirmDialog singleButton.
**Ф2-ShareDialog + Ф3-batch1 СДЕЛАНО (2026-06-06, dev):** ShareDialog больше НЕ руками собранный оверлей — сведён на общий Atlantic-примитив `Dialog` (убран двойной бэкдроп+сырой scrim). ВАЖНО про диалоги: «бошпоке» в основном на ОДНОМ примитиве Atlantic `Dialog`+`ModalHost` (императивный `window.__openModal`-стек) — это ВТОРАЯ система рядом с Radix-каноном; полная миграция ModalHost→Radix = крупный риск (десятки __openModal-вызовов), вынесена как стратег. решение, НЕ делал молча. Добавлен токен `--overlay-grad-soft` (light+dark). Связаны 6 точных совпадений inline→токен: модальные scrim rgba(15,23,42,.45)→`var(--scrim)` (TripView:857, Trips:152); оверлеи картинок→`var(--overlay-grad)` (CityPanel) и `var(--overlay-grad-soft)` (design/index:941, TripView:931, ManualPlanner:786). Babel+PostCSS зелёные.
**ОСТАЛИСЬ протечки = РЕШЕНИЯ ПО ВАРИАЦИЯМ (не угадывать):** AI-градиент `#6a3ee2→#c66ce2` (Trips ×3) + TriplanioAvatar `#2167e2→#8b3dff` → нужен токен AI-градиента; Telegram-бренд `#0088cc` (SettingsLens ×6) → токен `--tg` или трактовать как внешний бренд; Landing маркетинг (#ea4335 Google и пр.) → политика «внешние бренды вне системы»; лесенка аватаров AVATAR_COLORS/UserAvatar → есть токены `--avatar-1..8`, но в JS (нужен способ прокидки); booking-платформы `buildBookingPlatforms.jsx` (11 палитра-классов+hover) → внешние бренды; Trips:87 scrim .55; белые «костяшки» тогглов. Стратег. решение: ModalHost→Radix или санкционировать 2 системы.
**РЕШЕНО Pavel + СДЕЛАНО (2026-06-06, dev):** AI-градиент→токены; Telegram/booking→внешние бренды; аватары→8 цветов градиент единый источник; диалоги→ОДНА система = Atlantic-вид + Radix-движок под капотом (НЕ две системы; base44 тут ни при чём — Radix из shadcn-скаффолда нового фронта). Объяснил разницу систем.
**Ф3-batch2 СДЕЛАНО:** аватары — единый источник `src/lib/avatarRamp.js` (8 градиент-пар + avatarPair/avatarGradient); Avatar(design/index) и ScreenAccount переведены на него; **УДАЛЁН мёртвый `UserAvatar.jsx`** (нигде не импортировался, 16 сырых палитра-классов) — потребовалось `mcp__cowork__allow_cowork_file_delete` (rm в песочнице запрещён на mounted FS); убрана 3-я копия палитры+локальный hashStr в ScreenAccount (теперь цвета юзеров консистентны во всём app). Внешние бренды → `src/lib/externalBrands.js` (Telegram), 7×#0088cc в SettingsLens централизованы. AI-токены: добавлен `--ai-2` (#c66ce2), `--ai-grad`/`--assistant-grad` через var(); убраны CSS `--avatar-*` (источник = JS). Связаны ВСЕ AI-градиенты (Trips×3, TriplanioAvatar, ChatWidget×3, EventAiBlock×3, ScreenAccount, PanelAi×4, ForkPartnerModal, SettingsLens dock) → var(--ai-grad)/--assistant-grad/--ai/--ai-2. Babel(12 файлов)+ESLint(0 err)+PostCSS зелёные.
**⚠️ВАЖНЫЙ УРОК:** Mapbox paint (`setLineLayer color`) и canvas НЕ понимают `var(--…)` — нужен конкретный hex (FlowMap ROUTE_COLOR, ManualPlanner accent). Откатил ошибочную правку на hex. Эти hex — санкционированное исключение (whitelist гарда), как внешние бренды/SVG-fill.
**ОСТАЛОСЬ:** (кат. ДАННЫЕ-ВИЗ, отдельно) погодная палитра design/index:257, бюджет CAT_COLORS (BudgetLens:261) → решить через --chart-*; SVG-fill #8b3dff в TriplanioAvatar (иллюстрация); ValidationUI:121 hex-fallback. **Ф2-ДИАЛОГИ (ModalHost→Radix) СДЕЛАНО (2026-06-06, dev, статика зелёная, НУЖЕН браузер-смоук):** `ModalHost` (design/index.jsx) переписан: каждый `__openModal`-элемент обёрнут в `RadixDialog.Root/Portal/Overlay/Content` → focus-trap, Esc, scroll-lock, ARIA, click-outside бесплатно; Atlantic-вид сохранён (Overlay=`.dlg-backdrop` scrim→var(--scrim), Content=новый `.dlg-modal` центр-обёртка, внутри рендерится `.dlg`-карточка контента). `modal` только у верхней модалки стека. Сам примитив `Dialog` НЕ трогали (он рендерится и ВНЕ ModalHost — TelegramUnlinkDialog контрол., DocsLens пустое состояние — связывать его с Radix.Title нельзя, проверено). a11y-имя = sr-only «Triplanio» (TODO: реальный per-dialog title). 27 точек __openModal без изменений. ⚠️РИСК (не протестировано в рантайме): Radix outside-click может закрывать модалку при клике в портальный Radix Select/Popover внутри (AddExpense/CurrencySelect, ChangeRole) — обычно Radix layer-stacking это решает, но СМОУК-ТЕСТ обязателен. Откат = один файл (ModalHost).
**ОСТАЛОСЬ по диалогам:** 6 контролируемых на сыром shadcn ui/dialog (TripFormDialog, ServiceDialog, EventModal, EventEditDialog, ForkPartnerModal, TripLimitDialog) визуально отличаются от .dlg → следующий шаг: рестайл ui/dialog.jsx DialogContent/Overlay под .dlg-вид (1 файл, низкий риск) ИЛИ миграция на Atlantic <Dialog>.
**ТИПОГРАФИКА → LUMO, БАТЧ 1 СДЕЛАНО (2026-06-06, dev, статика зелёная, НУЖЕН браузер-ревью — крупная визуальная смена):** Pavel дал Lumo как стандарт типографики (файл DESIGN_SYSTEM_LUMO_2026-06-06.html). Шрифты Inter/Inter Tight → **Rubik (заголовки/кнопки 500/600/700) + Nunito (body/UI 400–800)** с кириллицей; заменены в 4 точках загрузки (index.html link, index.css/app.css/login.css @import) + токены (index.css --font-sans=Nunito/--font-display=Rubik; app.css --font-display=Rubik, --font-body ПЕРЕИМЕНОВАН в **--font-ui**=Nunito, 16 использований мигрированы; login.css локально). Лестница заменена на **Lumo-стандарт (имена+значения):** nano10·micro11·meta12.5·base14·strong15·h4 16·h3 19·h2 26·h1 40·display54. Старый дисплей-ярус переименован (lg→h4, xl→h3, title→h3, 2xl→h2, 3xl→h2, hero→display; 31 использование), h2/h1 сохранили имя но значение 22→26/32→40. База: body Nunito 15.5/1.6/вес500; h1–h4 Rubik 600 через токены, line-height Lumo, трекинг смягчён --display-tracking -0.022→-0.01. ⚠️ВСЁ приложение меняет шрифт и масштаб — Pavel должен глянуть в браузере.
**ТИПОГРАФИКА БАТЧ 2 СДЕЛАНО (2026-06-06, dev, статика зелёная):** ВСЕ хардкод-px размеры → токены `--fs-*` по ближайшему Lumo-значению (override 13px→base, т.к. это был дефолтный UI-текст=Lumo base 14). 101 размер в CSS (app.css+login.css; index.css уже на токенах) + 28 JSX `text-[Npx]`→`text-[length:var(--fs-*)]`. ВАЖНО: `ReadOnlyTimelineView` ОТКАЧЕН к исходным px (Pavel просил его игнорировать — отдельный рефактор; там 6 text-[] остались). Сырых font-size px/инлайн-fontSize больше НЕТ. PostCSS+Babel зелёные. Типографика под Lumo = ГОТОВО (кроме ReadOnlyTimelineView). Маппинг px→токен в коде комментариями не сохранён — при сомнениях см. лестницу Lumo.
**LUMO — ПОЛНЫЙ редизайн скоро:** Lumo это ПОЛНАЯ дизайн-система (там же --cat-1..8 для графиков=п5, цвета, радиусы --r-*, тени --sh-*, ev-цвета, primary, surfaces). Pavel: «остальные пункты — жди готовности Lumo». Значит цвета/радиусы/тени НЕ трогаем сейчас, ждём финальный Lumo. Сейчас только типографика.
**Прочие решения Pavel:** п6 (.ai-gradient CSS-классы) — токенизировать (скоро новый дизайн); внешние бренды — будут меняться/добавляться, нужен трекинг (реестр externalBrands.js — расширять); тёмная тема — уточнения будут в Lumo; гард (п9) — сделать последним.
**АВАТАР-БАГ ИСПРАВЛЕН (2026-06-06, dev):** причина мельтешения «градиент→плоский» — `src/lib/AuthContext.jsx` ГЕНЕРИРОВАЛ и СОХРАНЯЛ в БД dicebear-аватар (плоские инициалы) при отсутствии фото. Убрал генерацию (на создании профиля avatar_url = реальное OAuth-фото ИЛИ null; backfill-блок удалён). Добавил очистку легаси: если avatar_url содержит `api.dicebear.com` → null в объекте + best-effort `update(avatar_url:null)` в БД (само-лечится при следующем входе). Политика аватара: **фото → градиент (если фото нет), третьего нет.** `Avatar` (design/index) уже приоритезирует photo→image иначе avatarGradient(name). ScreenAccount «удалить аватар» ставит null → градиент. ⚠️существующим юзерам dicebear-URL чистится при следующем логине (клиентски сразу null).
**НЕ-LUMO ДОЛГИ ДОДЕЛАНЫ (2026-06-06, dev, Notion пропущен по просьбе Pavel):**
• .ai-gradient CSS-классы токенизированы (+токен `--ai-3` оранжевый; .ai-gradient/.ai-gradient-bg(color-mix)/.ai-gradient-border/.avatar--ai/.dock--ai/.ai-text → var(--ai/--ai-2/--ai-3/--ai-grad)).
• ui/dialog.jsx ЛЁГКИЙ рестайл под Atlantic-вид: Overlay scrim→var(--scrim)+blur (как ModalHost), Content bg-card/border-border/shadow-pop/rounded-2xl, Title +font-display. БЕЗ переструктуризации шапки/футера (глубокая унификация — Lumo-редизайн).
• **ГАРД готов:** `scripts/check-design-tokens.mjs` + `npm run check:design`. Типографика ENFORCED (валит при регрессе), цвета REPORT-ONLY (флаг COLOR_ENFORCED=false до Lumo). Whitelist: externalBrands, booking-platforms, Mapbox(FlowMap/MapView/ManualPlanner), SVG(TriplanioAvatar), AppErrorBoundary(crash-экран), token-def CSS, login.css, ReadOnlyTimelineView+StaySectionExpandable(отложены), BudgetLens/categoryStyles(data-viz ждут Lumo --cat). Гард сразу поймал пропущенный CalendarLens.css (font-size 19px→--fs-h3, починено). СЕЙЧАС PASSED.
**ИТОГ: всё, что можно БЕЗ полного Lumo — СДЕЛАНО.** Ждёт Pavel: браузер-ревью Lumo-типографики + смоук диалогов (Radix). Ждёт полный Lumo: миграция цветов/--cat-*/--ev-*/радиусов --r-*/теней --sh-* + ReadOnlyTimelineView + глубокая унификация диалогов + флип COLOR_ENFORCED=true в гарде. Token-fallbacks `var(--x, #hex)` в ValidationUI и т.п. — косметика, можно подчистить с Lumo. (187 классов кроме ReadOnlyTimelineView+отложен, 223 hex, inline-цвета→var, fs→--fs-*, avatar→--avatar-*, Pro→--pro). Затем ESLint-гард. Перед рефактором Pavel должен зафиксировать §5 решения (канон цвета на движок, ev-палитра, status-ramp, AI-акцент, avatar-ramp, Pro-токен, fs-ладдер+вопрос 9/10px, scrim-токены, объём Landing). Дорожная карта P0→P2 в §6, включая ESLint-гард против сырых классов/hex. Связано: [[triplanio-ui-audit]] (2 движка уже отмечались), [[triplanio-horizon-design-system]], [[triplanio-design-ux-audit]].
