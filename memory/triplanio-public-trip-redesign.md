---
name: triplanio-public-trip-redesign
description: Triplanio — анализ легаси публичного read-only экрана перед редизайном на Lumo (дизайн ждётся)
metadata: 
  node_type: memory
  type: project
  originSessionId: 325dc7b2-08dd-470c-9dbd-0807184606b7
---

★ТЗ СОГЛАСОВАНО (план) 2026-06-20, код НЕ начат. Редизайн публичного read-only экрана трипа на Lumo. Дизайн пришёл: TRIPLANIO_SHARED_TRIP_LUMO_v2_2026-06-20.html (журнальный «shared trip reader»: мастхед+лента флагов-стран → читалка(лента городов+липкая карта) → участники → CTA → футер). ТЗ-файл: «Triplanio docs/PUBLIC_TRIP_REDESIGN_TZ_2026-06-20.md».

РЕШЕНИЯ ПАВЛА (зафиксированы): (1) карта = Mapbox-монохром как в Статистике (НЕ D3/SVG из макета) — переиспользовать MapView + basemapTheme:'monochrome' (механизм из StatsMap) + focus-проп для скролл-фокуса (IntersectionObserver→focus), Mapbox-токен VITE_MAPBOX_TOKEN build-time доступен на публичке; (2) getPublicTrip адаптировать под контент: ДОБАВИТЬ owner{display_name,avatar_url}+members[{display_name,avatar_url,role}]+distance_km, поля захолитить (приватность, security-review), деплой prod+dev --no-verify-jwt; (3) хедер/футер — вынести LandingHeader/LandingFooter из LandingPage.jsx в общие SiteHeader/SiteFooter (тащит /landing.css, nav-якоря→triplanio.com, регресс лендинга); (4) ReadOnlyTimelineView УДАЛИТЬ + снять whitelist в check-design-tokens.mjs; (5) флаги /flags/${cc}.svg (паттерн из stats/widgets.jsx), отель-плашка условна, activities под городом; новые CSS-классы раскладки (mast/reader/cstop/cleg…) — на апрув. Фазы Ф1(edge)→Ф2(хедер/футер)→Ф3(страница)→Ф4(карта)→Ф5(снос+аудит).

ДОУТОЧНЕНИЯ ПАВЛА 2026-06-20: карта справа (как в макете); тема — следуем лендингу (светлая, БЕЗ тумблера темы из макета); новые CSS-классы раскладки одобрены.

ПРОГРЕСС (код, ждёт деплой+пуш): Ф1 ГОТОВ (код) — getPublicTrip расширен owner{display_name,avatar_url}+members[{display_name,avatar_url,role}], резолв из public-таблицы `users`(id,full_name,avatar_url,email), бот info@triplanio.com отфильтрован, email/user_id НЕ отдаются; ДЕПЛОЙ НЕ СДЕЛАН (вручную prod+dev `supabase functions deploy getPublicTrip --no-verify-jwt --project-ref <ref>`, потом list_edge_functions). Ф2 ГОТОВ — chrome вынесен в `src/components/site/SiteChrome.jsx` (SiteHeader/SiteFooter/useLandingCss, проп navBase для абсолютных якорей на публичке), LandingPage.jsx переведён на него, eslint обоих файлов зелёный. ОСТАЛОСЬ: Ф3 (новый PublicTrip.jsx по макету + scoped CSS под .ptrip на Lumo-токенах + i18n public.* en/es/ru; флаги /flags/${cc}.svg; SiteHeader navBase='https://triplanio.com/'), Ф4 (MapView справа + basemapTheme monochrome passthrough + focus по IntersectionObserver), Ф5 (удалить ReadOnlyTimelineView + снять whitelist в check-design-tokens.mjs + reuse-аудит + build/tests + смоук + push dev+main).

★РЕАЛИЗАЦИЯ 2026-06-20 (Ф1 edge задеплоен Павлом; FE lint+build зелёные, ждёт git push dev+main + git rm 6 файлов + смоук): Ф3 ГОТОВ — src/pages/PublicTrip.jsx переписан с нуля по макету (мастхед: kick+title+«Собрал {owner}»+лента флагов-городов+мета days/cities/countries/transfers/km; лента городов: флаг вместо кода, отель-плашка условна, activities под городом; переезды pill через transportInfo+formatDuration; участники из members; CTA; общий SiteHeader/SiteFooter navBase=triplanio.com; светлая тема как лендинг; EventModal/табы/обложка выпилены как мёртвый код) + src/pages/PublicTrip.css (scoped .ptrip/.pt-*, Lumo-токены, без новых токенов, адаптив 880/600) + i18n public.* (en/ru/es расширены). Ф4 ГОТОВ — MapView.jsx получил необязательный проп basemapTheme (проброс в useMapSurface, дефолт 'default', обратносовместимо); PublicTrip передаёт basemapTheme='monochrome' + focus по IntersectionObserver (камера на активный город). Ф5 ЧАСТИЧНО — whitelist ReadOnlyTimelineView снят в check-design-tokens.mjs; сами 6 файлов (ReadOnlyTimelineView, CityHero, StaySectionExpandable, TransferGroupReadOnly, CityNotesBlock, AddTransferButton — все импортились ТОЛЬКО из ReadOnlyTimelineView, подтверждено grep) физически НЕ удалены (песочница запрещает rm/git) → Павлу git rm; на сборку не влияют (нигде не импортятся). Reuse: MapView(+basemapTheme passthrough), useMapSurface monochrome, trip-stats/trip-cities/transport/time, /flags паттерн (FlagImg инлайн как в stats/widgets.jsx), Lumo-токены; new: .pt-* раскладка (одобрено Павлом) + SiteChrome (вынос из лендинга, анти-дубль). Смоук НЕ делался (свет/тёмная-нет/мобайл/битый токен/404).

★ФИКС ДИЗАЙН-ФИДЕЛИТИ 2026-06-20 (Павел: «дизайну не соответствует — ни шрифты ни цвета ни отступы; кнопки кастомные»). ДВЕ НЕОЧЕВИДНЫЕ ПРИЧИНЫ: (1) в проекте `--primary` = HSL-триплет shadcn (из src/index.css, типа "210 73% 47%"), пригоден ТОЛЬКО как `hsl(var(--primary))`; реальный цветовой бренд-токен = `--brand` (app.css: --brand=hsl(var(--primary))). Я писал `var(--primary)` как цвет → невалидно, акценты/номера красились мимо. `--primary-fg` в проекте НЕТ. Цветной токен бренда всегда `--brand`. (2) `/landing.css` (для shared chrome) инъектится в <head> в рантайме ПОСЛЕ бандла app.css, его `:root` перебивает `--font-display/--font-ui`/нейтрали глобально → body берёт шрифты/цвета лендинга. ФИКС: на `.ptrip` запинены Lumo-токены (значения из app.css: Rubik/Nunito, --brand #2173C8, --ink #272433, surfaces, line…) — body иммунен к landing.css, chrome (вне .ptrip) сохраняет landing-стиль; `var(--primary)`→`var(--brand)`; кастомные `.pt-btn` удалены, кнопки на готовых `.btn .btn--white/--primary/--lg/--sm` (из landing.css). lint+build зелёные. ВИЗУАЛЬНЫЙ СМОУК В БРАУЗЕРЕ ВСЁ ЕЩЁ НУЖЕН (в песочнице не отрендерить) — подтвердить на dev-деплое.

**Экран** = `src/pages/PublicTrip.jsx`, роут `/public/trip/:tripId?t=<token>` (App.jsx:35-38), без авторизации. Сейчас на Tailwind/shadcn-классах (bg-background/text-muted-foreground/font-display), НЕ Lumo. Структура: свой header (лого+«только просмотр»), обложка (фото/градиент getGradientById), заголовок+сабтайтл (даты/дни/города uniqueCityCount), табы Таймлайн/Карта, EventModal readOnly по клику. i18n namespace `public.*`.

**НЕ трогать (генерация ссылки/токена развязана с UI, контракт сохраняется):** ShareDialog.jsx (уже Lumo, точки входа TripView+TripStructureEdit) → edge `ensureShareToken` (owner-only, 32-hex в trips.share_token) → URL `/public/trip/${id}?t=${token}`. Чтение = edge `getPublicTrip` (canon-10 verify_jwt=false, при редеплое `--no-verify-jwt`; вырезает created_by+share_token; ответ {trip,visits,hotels,transfers,activities,carRentals}).

**Главное:** `ReadOnlyTimelineView.jsx` используется ТОЛЬКО в PublicTrip и ДУБЛИРУЕТ построение ленты, которое в основном экране делает общая `buildEventStream()` (TripView.jsx:73, её же юзает CalendarLens) + рисует Lumo `TimelineLens`. Т.е. уже 2 параллельные ленты, ReadOnlyTimelineView — старая (в whitelist check-design-tokens.mjs как «deferred refactor»). Внутри куча мёртвого edit-кода (canEdit/isEditMode/AddDayButton/EmptyTripCTA-AI) — из публички недостижим. Делать 3-ю ленту с нуля = нарушить reuse-first (Hard rule #6); правильно — свести публичную ленту на buildEventStream + read-only Lumo-карточки. **Решение об этом отложено Павлом «после дизайна».**

**Риски:** getPublicTrip отдаёт select('*') (booking_url/цены/заметки/все trip_services) — под дизайн решить какие поля показывать, не палить лишнее; сохранить naive-time семантику; showBookingWarnings=false оставить; EventModal readOnly + MapView переиспользовать.

★ИТЕРАЦИЯ 2 — план+карта 2026-06-22 (FE-only; lint+build+65 тестов зелёные; ЖДЁТ git push dev+main + браузер-смоук). 6 правок по запросу Павла:
(1) В itinerary теперь ВЕСЬ маршрут (start/finish/waypoint+transit), не только transit: `stops` строится из всего `ordered` (kind-aware), нумерация (1..N) ТОЛЬКО transit. transitStops (фильтр) идёт в ленту флагов мастхеда. Обозначения: start/finish = бейдж с глифом-флагом вместо цифры (`.pt-cstop__num--icon` + `RoleGlyph`, те же SVG-пути что у маркеров карты markers.js) + роль-тег `.pt-role` (Старт/Финиш); waypoint = тонкая ghost-строка `.pt-cstop--wp` (кольцо-точка на рейле+флаг+город+тег Транзит, без номера/карточки). i18n public.role_start/role_end/role_waypoint en/es/ru.
(2) Карта поднята под разделитель шапки: мастхед+статистика+лента городов сгруппированы в ЛЕВУЮ колонку (`.pt-head`), карта `.pt-mapcol` спанит обе строки грида справа (sticky top:84). Мобайл (≤880): порядок head→map→itin через order.
(3) На загрузке (фокус наверху) первый город НЕ выделяется: `focusIdx=-1` → MapView показывает ВЕСЬ маршрут (все маркеры+линии, общий fit), focus=null. При скролле вниз focusIdx→0, камера летит.
(4) Прогрессивная отрисовка (новая аддитивная возможность MapView, пропсы `revealActiveId`/`revealProgress`): markers до active видны, дальше скрыты (el.style.display, без новых CSS-классов); линии — `drawRouteReveal()` в routeLines.js (legs<active полные, leg=active нарезается sliceLine по progress; реюз geodesicLine/roadCache/setLineLayer). Камера = пошаговый flyTo (focus), линия = по прогрессу скролла (Павел просил «и 1 и 2»). revealActiveId=null ⇒ старое поведение (все прочие потребители MapView: ScreenMap/TripStructureEdit/RouteMapCard не затронуты). Робастность: если active-город без координат (revealActiveIdx<0) ⇒ откат к полному маршруту.
(5) Баг «при скролле вверх города через один»: IntersectionObserver заменён детерминированным scroll-расчётом (rAF-throttle): active = последний `.pt-cstop` чей top≤45%vh, progress=доля анкора между active и next. Пропусков нет в обе стороны.
(6) Бейдж-пилюля внизу-слева карты `.pt-mapcap__pill` красится в `accent` активного города (как бейдж в таймлайне), не в --brand. accent: transit=ACCENTS-цикл по номеру, start=--brand, end=--ev-transfer, waypoint=--muted.
Файлы: routeLines.js(+drawRouteReveal/legGeometry/sliceLine), MapView.jsx(+2 пропа, legs/lineSig вынесены в useMemo, отдельные эффекты линий+видимости маркеров), PublicTrip.jsx(+RoleGlyph, stops/transitStops, scroll-движок, layout, reveal-пропсы), PublicTrip.css(.pt-top/.pt-head грид, .pt-role/.pt-cstop--wp/.pt-wp__*, мобайл-order), i18n ×3. Reuse audit: reused MapView/useMapSurface/routeLines-геометрия/markers-глифы/Lumo-токены/.pt-* существующие; new: пропсы revealActiveId/revealProgress+drawRouteReveal (одобрено через выбор механики), .pt-role/.pt-cstop--wp/.pt-wp-* (одобрено: «иконки вместо цифр+ghost-waypoint»). ДОЛГ: check:design «типографика» падает на PublicTrip.css — это ПРЕДСУЩЕСТВУЮЩИЕ хардкод-px (файл зеркалит landing), мои 3 новых правила той же конвенции; разруливается общим Lumo-проходом, не этой задачей. Браузер-смоук НЕ делался (нужна залогиненная вкладка/dev-деплой): проверить top=весь маршрут, рост линии+появление маркеров по скроллу, отсутствие пропуска вверх, цвет pill, мобайл-порядок.

★ИТЕРАЦИЯ 3 — фидбэк Павла по скриншотам 2026-06-22 (резко: «разворотил страницу»), всё поправлено (lint+build+65 тестов зелёные, ждёт push). 4 правки: (a) РАЗДЕЛИТЕЛЬ: «под разделитель шапки» = НЕ под site-header, а под линию-сепаратор после заголовка/автора. Раскладка переразбита: `.pt-mast` (kick+title+автор) теперь ПОЛНОШИРИННАЯ секция сверху; ниже отдельная секция `.pt-reader` с `border-top` (=разделитель) и 2 колонками — слева `.pt-left` (лента городов+статистика+`.pt-itin`), справа `.pt-mapcol` (sticky, стартует под разделителем). h1 вернул полноширинный clamp(38,6.5vw,78). (b) Карта больше НЕ выталкивается: огромный заголовок ушёл из колонки наверх; грид `minmax(0,1fr) minmax(360px,40%)` gap44; статистика `.pt-meta` ужата (minmax116, gap20/22, без max-width) в левой колонке. (c) НАЛОЖЕНИЕ статистики на первый город: убран грид-row трюк (был `grid-template-rows:auto 1fr` + спан карты — он и давал overlap); `.pt-itin` получил `border-top+margin/padding-top` — чистое разделение meta↔первый город. (d) АНИМАЦИЯ линии переписана: была привязка к прогрессу скролла (резкое появление + предрисованные будущие легы — Павел отверг). Теперь рост линии — ВНУТРИ MapView по таймеру, синхронно с камерой flyTo: при продвижении активного индекса i→i+1 анимируется ТОЛЬКО лег (i→i+1) за `focusDuration` (easeOutCubic, rAF, internal `anim{legIdx,prog}` state); пока фокус стоит на городе — лег к следующему НЕ рисуется (no pre-drawn future legs); маркер следующего города (`markerMax=legIdx` во время анимации) появляется в конце роста. Проп `revealProgress` УДАЛЁН (и из MapView, и из PublicTrip); PublicTrip больше не считает scroll-fraction, только focusIdx. Reverse-скролл = мгновенный снап (без обратной анимации). markerMaxRef/revealingRef для build-эффекта. Мобайл (≤880): карта order:-1 над левой колонкой.
★ИТЕРАЦИЯ 4 2026-06-22: таймер-изинг (easeOutCubic) рассинхронил линию с камерой (линия обгоняла на старте, камера приходила первой). ФИКС: доля линии теперь = проекция ЖИВОГО центра камеры (`map.getCenter()` на событиях `move`) на сегмент текущего лега A→B (param t=clamp(((c−A)·(B−A))/|B−A|²)); голова линии физически на камере, рассинхрон невозможен; на `moveend` — settle (лег полный, маркер цели появляется). Камера осталась на `flyTo` (focusDuration=4200). Маркер цели всплывает с pop-анимацией (WAAPI на `.tmk__core`: scale 0.2→1.12→1, 340ms, без новых общих CSS).
★ИТЕРАЦИЯ 5 2026-06-22: move-привязка через React-state (`anim`) давала гонки (3 бага: вся полиния мелькала и пропадала / линия+маркер появлялись моментально без анимации / нестабильно). ФИКС-АРХИТЕКТУРА: 3 раздельных эффекта (anim-state + lines + visibility) СВЁРНУТЫ в ОДИН императивный reveal-контроллер в MapView. Состояние в `revealStateRef` (НЕ React-state){animLeg,animProg,fullLegs,markerMax,revealing}; локальная `paint()` рисует линию (`drawRouteReveal`) + видимость маркеров (`applyMarkerVisibility`, вынесена на module-level, общая с build-эффектом) императивно — per-frame апдейты НЕ вызывают ререндер. Контроллер ВЛАДЕЕТ камерой целиком: forward i→i+1 = flyTo + move-проекция центра на лег (rev `paint` каждый move); снап (top→0/назад/прыжок); !revealing = полный маршрут + fit назад. focus-эффект гард `if(revealActiveId!=null)return`; PublicTrip БОЛЬШЕ НЕ передаёт `focus`/`focusDuration` (контроллер сам летает), focusPts удалён. ДЛИТЕЛЬНОСТЬ ПЕРЕЛЁТА от расстояния: `revealFlyDuration`=clamp(1600..7000, 1400+sqrt(legKm)*62) — Москва→Нью-Йорк теперь ~6.5с, короткий хоп ~1.6с (`legKm`=haversine, module-level). Удалены drawActiveIdx/drawProg/markerMax-derived, revealingRef/markerMaxRef, anim-state, 2 эффекта. lint+build+65 тестов зелёные.
★ИТЕРАЦИЯ 6 2026-06-22: move-проекция flyTo всё равно глючила (линия моментально при прерывании скролла до остановки зума; редкий флеш; сильное торможение в финале от ease-out flyTo). ПЕРЕПИСАНО на ОЧЕРЕДЬ анимаций (queue pump) с РУЧНЫМ rAF, БЕЗ flyTo/move-событий для роста: камера и линия идут по ОДНОМУ прогрессу p (ЛИНЕЙНО — нет торможения в финале); камера = голова линии через новый `legPointAt(from,to,kind,f)` в routeLines (точка на дуге на доле f, та же что у среза линии) → идеальный синк. Анимируется ПО ОДНОМУ легу за раз (reachedRef→targetRef), очередь догоняет быстрый скролл — каждый лег проигрывается целиком, без мгновенных снапов. genRef-токен абортит pump при скролле назад/выходе; pump переживает ререндеры (cleanup эффекта НЕ отменяет, только unmount-эффект). Зум-параболу делаю сам: `zoom = REVEAL_CITY_ZOOM - dip*sin(pi*p)`, dip от cameraForBounds лега (длинные легы отъезжают дальше). Длительность от расстояния `revealLegDuration`=clamp(2000..8000, 1700+sqrt(km)*72). ЗУМ ГОРОДА отъехал: `REVEAL_CITY_ZOOM=5.6` (было 9.2). Скролл-назад=снап+flyTo. PublicTrip не передаёт focus (как в ит.5). Файлы: routeLines.js(+legPointAt), MapView.jsx. lint+build+65 тестов зелёные; браузер-смоук за Павлом. Файлы те же (MapView.jsx, PublicTrip.jsx, PublicTrip.css). Reuse: всё та же дизайн-система; new: только внутренняя anim-логика (не новый компонент/токен).

Связано: [[triplanio-deploy-verify-jwt]] [[triplanio-getpublictrip-idor]] [[feedback-reuse-first-unification]] [[triplanio-horizon-design-system]] [[triplanio-map-singleton]] [[triplanio-map-markers-ring]]
