---
name: triplanio-stats-dashboard-redesign
description: Triplanio — редизайн экранов «Поездки»(главная)+«Моя статистика» и жёсткий фидбэк Павла по дизайну
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 535c6841-d374-4413-83d3-5c84b4326ab6
---

★2026-06-19. Экраны статистики/дашборда. Итог — `Triplanio design new/TRIPS_STATS_REDESIGN_LUMO_V3_2026-06-19.html` (v1/v2 отвергнуты).

**Жёсткий фидбэк Павла (применять ко ВСЕМ дизайн-макетам Triplanio):**
- ВСЕГДА вязаться к `Triplanio design new/LUMO DESIGN SYSTEM.html` (302KB, реальная DS): токены, `.btn/.seg/.panel/.sec-head`, ховеры строго `@media(hover:hover)`+`translateY(-2/-3/-5px)`+`box-shadow:var(--sh-2/3)`+`--line-hover`, hero-blob атмосфера (размытые градиентные круги), `.tc` с `.tc__blob`, типографика display=600 / body=500. НЕ изобретать свой визуал.
- НИКАКИХ «дефолтных тетрисов» — сеток одинаковых бордюрных квадратов. Цифры — на мягких wash-полках/хейрлайнах, воздушно, с whitespace. Повторяющаяся претензия.
- Карта — ГЕРОЙ (эмоц. центр travel-статистики), крупная, светлая. На мобиле карта — СВЕРХУ, не под блоками.
- Лёгкость/воздух/современность/приятно смотреть — приоритет над плотностью.
- НЕ заворачивать статистику на главной в один контейнер-карточку.
- Пустое состояние = реальный каркас экрана с нулями (призрачная карта, 0-счётчики, кольцо 0%, рекорды «—») + создание трипа. НЕ одна общая заглушка вместо всего. Едино на обоих экранах.
- Карточки кликабельны целиком — без отдельной кнопки «Открыть» у след. трипа.
- Мобильный боттом-нав прибивать к НИЗУ фрейма (фрейм = fixed height + внутренний скролл `.main`), не «висеть по центру».
- Запрещённое слово: «Дашборд путешественника».
- Светлая тема для этих экранов.

**Содержание статистики (считается из данных, реагирует на год-фильтр):** страны/города/континенты/поездки/переезды/км; «мир исследован %» (страны/195) — бар на главной + кольцо на статистике; континенты-бары; рекорды (дней в пути=сумма ночёвок, вокруг Земли ×км/40075, чаще всего=топ-страна, самая длинная=макс км); график по годам. Открытые вопросы: набор тайлов, «дней в пути» (ночёвки vs календарь), знаменатель 195.

**★2026-06-19 ИНЖ-ТЗ согласовано (код НЕ начат), макет V4 `TRIPS_STATS_REDESIGN_LUMO_V4_2026-06-19.html`, ТЗ-файл `Triplanio docs/TRIPS_STATS_REDESIGN_TZ_2026-06-19.md`:**
- Карта = Mapbox их тайлы, тот же app-wide синглтон (`MapProvider`/`useMapSurface`) что и трип-экраны — НЕ d3/topojson (был бы 2-й движок). Новое минимально: +1 fill-слой заливки стран (country_boundaries feature-state по iso_3166_1, цвета токены --map-trip/manual/future), на трип-экранах слой `visibility:none`, на главной/статистике видим. Пины DOM-маркеры уже подменяются per-screen, фокус уже per-screen — переиспользуем как есть.
- Дистанция УБРАНА из дизайна совсем (нет общ.дистанции, нет «вокруг земли»). Переезды: на ГЛАВНОЙ «переезды(кол-во)» = ЖИВАЯ цифра (RPC `transfers_total`=count трансферов, без деталей); на СТАТИСТИКЕ перелёты+наземные(сплит по типу)=ПЛЕЙСХОЛДЕРЫ (детали транзитов пока не возвращаем). Рекорды (4): дни в трипах(из дат) / любимый город(макс посещений) / любимая страна / самая длинная(по uniqueCityCount). Доп.UI: тоггл полноэкранной карты на статистике. Знаменатель «мир исследован»: Mapbox country_boundaries заливает ~249 ISO-кодов(+2 неофиц, вкл. тер-рии типа Гонконга)≠195 — ОТКРЫТО: единый знаменатель 195 vs ~249.
- RPC `get_user_travel_stats()` СРАЗУ → отдаёт компактные points [{id,kind:'trip'|'custom',trip_id,city_name,country_code,lat,lng,start_date,end_date}] + trips-словарь; год-фильтр и ВСЕ агрегаты на клиенте (нулевая сеть на смену года, «за всё время»=без фильтра); заменяет нынешний `select * city_visits` на главной (легче). Счёт НЕ дублировать в SQL.
- Счёт через существующий `trip-cities.js` (`uniqueCityCount`/`uniqueCountryCount` — НЕ новая логика). «Самая длинная»=max(uniqueCityCount).
- Новая таблица `user_custom_visits` (user_id FK,city_name,country_code,lat,lng,start_date,end_date) + RLS по auth.uid(); геокод через общий geoLocationiq+geocode_cache. Статистика+ручные посещения = FREE, без лимита.
- Континенты: статический JS `src/lib/continents.js` (ISO2→континент ~250), без БД. Агрегатор `src/lib/travel-stats.js` поверх trip-cities/trip-stats.
- Новые элементы под approval rule#6 (одобрены Павлом): user_custom_visits, RPC, fill-слой карты, travel-stats.js, continents.js.

**★РЕАЛИЗАЦИЯ (ветка `feature/trips-stats-redesign` от dev, запушена):**
- Ф1 ГОТОВО+ЗАДЕПЛОЕНО prod+dev: миграция `0042_user_custom_visits_and_travel_stats.sql` (переименована с 0041 из-за коллизии с чужим WIP `0041_geocode_token_bucket.sql`) — таблица `user_custom_visits`(RLS auth.uid, 4 политики) + RPC `get_user_travel_stats()` отдаёт {points:[{id,kind:'trip'|'custom',trip_id,city_name,country_code,lat,lng,start_date,end_date}], trips:{id:{title,cover_gradient,cover_image_url}}, transfers_total}. «Мои трипы»=owner∪active trip_members (как is_trip_participant). Транзит-точки kind='transit'. Проверено на dev.
- Ф2 ГОТОВО (в ветке): `src/lib/continents.js` (ISO2→континент AF/AS/EU/NA/SA/OC/AN, continentOf()), `src/lib/travel-stats.js` (агрегатор поверх trip-cities.cityKey: pointYear/availableYears/filterByYear/pointType('manual'/'future'/'trip')/countCities/Countries/Continents/Trips/worldExplored(195)/countriesList/citiesList/continentsBreakdown/tripsByYear/daysInTrips/favoriteCity/favoriteCountry/longestTrip/homeStats/statisticsBundle), `travel-stats.test.js` 6/6 зелёные, ESLint чисто.
- ОСТАЛОСЬ: Ф3 карта (fill-слой country_boundaries v1 как отдельный vector source `mapbox.country-boundaries-v1`, promoteId iso_3166_1, feature-state цвет; тоггл видимости трип=none; пины 3 цветов через createMarkerEl из markers.js; fullscreen=переезд слота acquire). Ф4 главная. Ф5 экран статистики. Ф6 i18n+перф. Карта на Mapbox Standard (mapbox.js MAP_STYLE) — fill требует браузерной проверки (slot/порядок слоёв).
- Грабли среды: git в песочнице залочен (HEAD.lock неудаляем, FS Operation not permitted) — коммиты делает Павел локально по командам.

**★2026-06-20 PR-A (read-only Ф5) НАПИСАН (lint+52 теста+build зелёные, i18n паритет 69/69/69; ЖДЁТ браузер-смоук + git-пуш Павла dev+main):** решения сессии — «всё одним PR», но «Добавить место» (запись user_custom_visits+геокод) ВЫНЕСЕНО в отдельный PR-B позже (read-only показывает уже существующие kind:'custom'). Файлы: `app.css` (+блок Ф5 классов из V4, токены только существующие, БЕЗ новых токенов), `components/stats/widgets.jsx` (+SummaryTiles/WorldRing/ContinentBars/Records/YearChart/VisitList+иконки), `components/stats/VisitPanel.jsx` НОВЫЙ (Radix Dialog как Sheet: десктоп правый slide-over / моб bottom-sheet <640px), `components/views/StatsMap.jsx` (+projection-проп для глобус-тоггла, onCountryClick по fill-слою, 3-цветные tone-пины через dominantTone, sizeSignal→resize для fullscreen), `lib/map/markers.js` (+опц. `tone`→`.tmk--t-*`), `lib/map/countryFill.js` (+экспорт COUNTRY_FILL_LAYER), `pages/Statistics.jsx` (полный экран: год-фильтр клиентский, summary 6 тайлов [перелёты/наземные=плейсхолдеры «—» soon, т.к. сплит транзитов не отдаём], world+континенты, списки стран/городов+VisitPanel, рекорды, график по годам, пустое=каркас-нули+empty-note CTA), i18n ru/en/es +69 ключей. РЕШЕНИЯ реализации: страны-имена через `Intl.DisplayNames(locale,{type:'region'})` (без таблицы имён); континенты статикой CONT_ORDER/CONT_COLOR на event-токенах; fullscreen = CSS `.mapwrap.is-fs` fixed-overlay того же слота синглтона (НЕ reparent) + resize; пин-tone trip=--primary/manual=--ev-car/future=--ai; country-fill оставлен ОДНОцветным (routeColor), 3-цветность только на пинах (как и было заложено). ОТКЛОНЕНО от V4: map-local theme-кнопка (избыточна — карта следует глоб.теме), share-кнопка, «Добавить место» (→PR-B). ОТКРЫТО/риск: браузер-смоук (fill-слой/контролы/fullscreen-resize не ломают трип-экраны — синглтон); плюрализация subs обойдена форматом «городов: N»/«Посещений: N» (i18n t() без Slavic-плюралов).

**★2026-06-20 PR-B (ручные места «Добавить место») НАПИСАН (lint+52 теста+build зелёные; ЖДЁТ браузер-смоук + git-пуш dev+main):** чисто фронт — бэк (таблица `user_custom_visits` + 4 RLS-политики auth.uid() + custom-ветка RPC, отдаёт `id`=ucv.id) уже в миграции 0042. НОВЫЙ `components/stats/AddPlaceDialog.jsx`: Dialog (Lumo) + переиспользован `CitySearch` (автокомплит LocationIQ + обязательная GeoAttribution; отдаёт city_name/country_code/latitude/longitude — отдельный геокод НЕ нужен) + `Input type=date`; insert/update/delete `user_custom_visits` с ЯВНЫМ user_id (RLS-insert требует =auth.uid()), `useToast`, `qc.invalidateQueries(['travel-stats',uid])`. `VisitPanel`: +проп onEditManual → manual-строка стала кнопкой «Изменить» (без onEditManual — read-only «Добавлено вручную», как в PR-A). `Statistics`: кнопка «Добавить место» в шапке (.sec-actions), empty-CTA→openAdd (empty_sub/empty_cta переписаны на manual-формулировку), openEditManual закрывает панель и открывает диалог в edit-режиме по point.id. i18n ru/en/es +15 ключей (паритет 84/84/84). Безопасность: запись только своих строк (RLS using/ with check user_id=auth.uid()); удаление/правка по id безопасны от IDOR (RLS). НОВЫХ CSS-классов/токенов нет (чип города инлайн-стилями). Reuse: Dialog/Btn/Field, CitySearch+GeoAttribution, Input, useToast, countryFlag, travel-stats RPC.

См. [[feedback-reuse-first-unification]], [[triplanio-horizon-design-system]], [[triplanio-bottomnav-header-unification]], [[triplanio-map-singleton]], [[triplanio-geocode-cache]], [[triplanio-map-markers-ring]], [[triplanio-trip-limit-sources]].
