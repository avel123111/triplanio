# Что graphify НЕ индексирует. Синтаксис как у .gitignore; сам .gitignore
# уважается автоматически, здесь только то, что лежит в git, но кодом проекта
# не является. Правило: в графе — только наш рабочий код. Остальное портит
# и кластеризацию, и счётчики «сколько раз это используется».

# Вендоренные чужие скиллы: ~90 узлов из примеров в чужих репозиториях.
.claude/

# Песочница инспектора канонов, в сборку приложения не входит.
dev/

# Архив миграций до схлопывания в baseline (TRIP-102): 179 узлов истории,
# которую уже не накатывают. Видно только при установленной грамматике SQL.
supabase/migrations_legacy/

# Строки локалей. tree-sitter даёт по ним 0 узлов (это плоские данные, а не
# код) и 132 предупреждения на каждую сборку. Мёртвые i18n-ключи графом НЕ
# ищутся — см. SKILL.md и memory/feedback-dead-i18n-key-sweep-must-scan-backend.md.
src/lib/i18n/locales/
