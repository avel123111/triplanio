/**
 * ПЕРИМЕТР ЭПИКА (TRIP-321/337 §10) — один предикат на всех, кто судит разметку
 * и стили дизайн-слоя.
 *
 * ЧТО ЭТО. Лендинг, экран входа и публичные правовые страницы унификацией НЕ
 * трогаются до фазы 10. Это не «забыли», а решение: они портированы отдельной
 * статикой, живут своей типографикой и своим `.card`/`.btn`, и втягивать их в
 * канон раньше времени значит менять маркетинговую страницу ради числа в гарде.
 *
 * ПОЧЕМУ ОТДЕЛЬНЫМ ФАЙЛОМ. Предикат родился в `audit-design.mjs` и там же
 * применялся, поэтому гард 2q, написанный позже, судил разметку, которую эпик
 * из периметра ИСКЛЮЧИЛ: `btn--lg`/`btn--white` (LandingPage, PublicTrip) и
 * `card--wide` печатались как «класс не существует, элемент МОЛЧА получит
 * дефолт» - заведомо ложно, правило у них есть в `public/site.css`, который
 * инжектится рантаймом на маркетинговых роутах (`SiteChrome.useSiteCss`).
 * Два гарда, отвечающие на соседние вопросы по одному дереву, обязаны иметь
 * ОДИН периметр, иначе расхождение молча становится вердиктом.
 *
 * ⚠️ ЦЕНА, И ОНА ОСОЗНАННАЯ. Исключение симметрично: 2q перестаёт проверять на
 * лендинге не только ложные случаи, но и ЗАКОННЫЕ (`btn--primary`, `btn--ghost`
 * там тоже есть). Лендинг не судится НИ В ОДНУ СТОРОНУ до фазы 10, и снимается
 * это одной строкой в одном месте - в чём и смысл общего предиката.
 *
 * ⚠️ ГРАНИЦА ПРИМЕНЕНИЯ. 2q применяет предикат к скану РАЗМЕТКИ. К скану CSS -
 * нет, и сегодня это разницы не даёт: из CSS-файлов `src/**` под предикат
 * попадают ровно два - `pages/login.css` и `pages/PublicTrip.css` (всего под
 * него подходит 6 файлов, остальные 4 - разметка), а канон-обличий
 * (`<семья>--<хвост>`) в этих двух ЗАМЕРЕНО НОЛЬ. Появится первое - предикат надо будет применить и там;
 * пока применять его к CSS значило бы объявить решение, которого никто не
 * принимал. `public/site.css` вне периметра и так - он вне `AUDIT_ROOT`.
 */

/** ⚠️ ПЕРИМЕТР РАСХОДИЛСЯ САМ С СОБОЙ, И ЭТО БЫЛО ВИДНО ТОЛЬКО ПОШТУЧНО.
 *  `login.css` стоял в списке, а `Login.jsx` - нет: СТИЛИ экрана входа не
 *  судились, РАЗМЕТКА судилась. Пока периметр применяли к счёту классов, цена
 *  была незаметной; с TRIP-337 редакции 4 приёмка объекта звучит «сырых тегов
 *  этого объекта в периметре НОЛЬ», и такой экран не пересадить В ПРИНЦИПЕ:
 *  чтобы снять с приватного класса облик, надо тронуть файл, который трогать
 *  нельзя. Одна половина предиката требовала работы, вторая её запрещала.
 *
 *  Решение Pavel 2026-08-09: лендинг · вход · join · публичная · terms ·
 *  privacy - ВНЕ СКОУПА ЭПИКА ЦЕЛИКОМ, по ним отдельная работа (а не «до
 *  подзадачи 10», как читалось раньше). Здесь приведён в соответствие код.
 *
 *  ⚠️ ЦЕНА НАЗВАНА ВСЛУХ, ЧТОБЫ ЕЁ НЕ ЗАСЧИТАЛИ ЗА ПРОГРЕСС. `Login.jsx` -
 *  самый тяжёлый файл репозитория по сырой разметке (203 тега, из них 19
 *  `<button>`), поэтому его вывод из ЗНАМЕНАТЕЛЯ поднимает долю «собрано из
 *  ДС» примерно на 1.7 п.п. БЕЗ ЕДИНОЙ СТРОКИ РАБОТЫ. Это КОРРЕКЦИЯ БАЗЫ, а не
 *  движение: храповик 2o такое повышение пропускает по построению (доля растёт
 *  только вверх), и ровно поэтому оно обязано быть названо в теле PR - иначе
 *  следующий заход отчитается чужим числом. Симметрично падают числа, которые
 *  храповятся ВНИЗ (классы, инлайны, приватная раскладка): их понижение здесь -
 *  тоже не заслуга.
 *
 *  Файлы, которых унификация TRIP-321 не касается (лендинг / вход / публичные).
 *  В `audit-design.mjs` они всё ещё СКАНИРУЮТСЯ на определения алиасов - остров
 *  токенов там ровно то, что делает глобальное переименование небезопасным, - но
 *  исключены из счёта семей и классов.
 *
 *  ⚠️ ЗАПИСЕЙ ДВА РОДА, И ГРАНИЦА У НИХ РАЗНАЯ - ЭТО НЕ НЕДОСМОТР (ревью Codex).
 *  `login.css` и `Login.jsx` - КОНКРЕТНЫЕ ФАЙЛЫ, поэтому сегмент обязан кончиться
 *  вместе с ними (`(?=$)`). Остальные - ПРЕФИКСЫ ИМЕНИ, и хвост им нужен:
 *  `Landing` обязан ловить `LandingPage.jsx`, `PublicTrip` - и `.jsx`, и `.css`.
 *
 *  Без правой границы `Login.jsx` матчился началом сегмента, то есть исключал и
 *  `Login.jsx.test.js`, и всё под каталогом с таким именем. Цена промаха
 *  НЕСИММЕТРИЧНА и потому опасна: лишний исключённый файл уезжает из ЗНАМЕНАТЕЛЯ,
 *  доля «собрано из ДС» от этого РАСТЁТ, 2o храповит её только вверх - дефект
 *  предиката выглядит как прогресс и не краснеет нигде. Ровно то же и у 2q: он
 *  просто перестаёт судить разметку, оставаясь зелёным. */
// SiteTrip — примитивы публичной поездки (SiteHero/SiteSummary/SiteCta),
// вынесенные из PublicTrip.jsx (TRIP-461). Это ТА ЖЕ зонная разметка на сайтовой
// ДС (`pt-*`, `.btn.btn-light` из public/site.css), что и остальная зона — вне
// скоупа эпика, как SiteChrome и Landing. Без этой записи вынос компонента молча
// втянул бы публичку обратно в периметр (2z ловил `<a .btn.btn-light>`).
export const OUT_OF_SCOPE = /(^|\/)(?:(?:login\.css|Login\.jsx)(?=$)|PublicTrip|JoinTrip|SiteChrome|SiteTrip|Landing|Privacy|Terms)/;

export const inScope = (f) => !OUT_OF_SCOPE.test(f);
