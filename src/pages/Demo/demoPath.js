// Адрес демо-поездки — ОДНА константа на всё приложение.
//
// Её знают трое: лендинг (кнопка «посмотреть демо» и адресная строка в мокапе
// браузера), маршрутизация (`App.jsx`) и карта сайта. До этого слаг был вписан
// строкой в трёх местах лендинга — то есть переименование демо ломало ссылку
// молча, потому что менялась не каждая копия.
//
// ★ Отдельный файл, а не константа в `demoTrip.js`: данные демо приезжают
// отдельным чанком (`lazy` в App.jsx), и импорт ради одной строки затащил бы их
// в бандл лендинга — ровно то, от чего этот `lazy` и стоит.
//
// Маршрут объявлен ТОЧНЫМ (`path={DEMO_PATH}`, не `/d/:slug`), поэтому любой
// другой слаг проваливается в `*` и получает 404 вместо демо под чужим адресом.
// Карту сайта правим руками — это статический XML, импортировать её нечем.
export const DEMO_PATH = '/d/europe-may-2027';
