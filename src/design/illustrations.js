// ─── Реестр иллюстраций (TRIP-532) ───────────────────────────────────────────
// ОДНО место, где живут путь и собственные размеры каждой картинки-иллюстрации
// приложения: маскот Боно, предметные сцены карточек, будущие комбинированные.
// Файлы лежат в `public/images/` (та же схема, что `public/covers/fallback.webp`
// и `public/flags/`): WebP с альфой, универсальные для обеих тем — фон под
// картинкой рисует поверхность-владелец, а не сама картинка.
//
// Размеры нужны `<img width height>`: без них вёрстка прыгает, пока файл едет.
// Ширина/высота = ФАКТИЧЕСКИЕ пиксели файла (2× плотность под ~половинный
// показ) — тест `illustrations.test.js` сверяет их с заголовком WebP, чтобы
// число здесь не разъехалось с файлом молча.
//
// Новая картинка в проекте = файл в `public/images/` + одна строка здесь.
// Имя — «что-где» по смыслу (`create-ai`), не по типу («mascot-…»): в одной папке
// живут и маскот, и предметы, и их комбинации.

/** @type {Readonly<Record<string, { src: string, w: number, h: number }>>} */
export const ILLUSTRATIONS = Object.freeze({
  /** Выбор способа: «Собрать вручную» — строки маршрута, которые тащат курсором. */
  'create-manual': { src: '/images/create-manual.webp', w: 384, h: 384 },
  /** Выбор способа: «Начать с Triplanio» — сообщение и ответ Боно с черновиком маршрута. */
  'create-ai':     { src: '/images/create-ai.webp',     w: 384, h: 384 },
  /** Герой пустого экрана «Мои путешествия»: Боно на чемодане, «собрался, жду». */
  'bono-ready':    { src: '/images/bono-ready.webp',    w: 336, h: 600 },
  /** Экран «Путешествие создано» (StepReview): Боно машет лапой, чемодан с галочкой. */
  'trip-created':  { src: '/images/trip-created.webp',  w: 480, h: 480 },
  /** Аватар бота во всех чатах (`<Avatar kind="ai">`): портрет Боно в очках-авиаторах
   *  на градиенте — круг без внешнего ободка (обод срезан: у `.avatar` своя рамка).
   *  128 = 3× от 38 px ленты; 6 КБ. */
  'bono-avatar':   { src: '/images/bono-avatar.webp',   w: 128, h: 128 },
  /* Виды точки маршрута (`cities/CityAdder`, плитки выбора). Четыре сцены одной
     серии: у них общий герой и общий масштаб кадра, поэтому холст у всех
     одинаков (288², 3× под ступень `--art-tile` 96) и НЕ обрезан по фигуре —
     обрезка по своему bbox развела бы рост Боно от плитки к плитке. */
  /** Вид точки «Посещение»: Боно гуляет по городу — остановка с ночёвками. */
  'point-transit':  { src: '/images/point-transit.webp',  w: 288, h: 288 },
  /** Вид точки «Пересадка»: Боно ждёт под табло вылетов — день без ночёвки. */
  'point-waypoint': { src: '/images/point-waypoint.webp', w: 288, h: 288 },
  /** Вид точки «Старт»: Боно выходит за дверь с чемоданом — начало поездки. */
  'point-start':    { src: '/images/point-start.webp',    w: 288, h: 288 },
  /** Вид точки «Финиш»: Боно разбирает чемодан — конец поездки. */
  'point-end':      { src: '/images/point-end.webp',      w: 288, h: 288 },
});
