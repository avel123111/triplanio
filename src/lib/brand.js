// Single source of truth for the app's brand name on the frontend.
// Keep in sync with functions/_shared/emailTemplate.ts (BRAND constant).
export const BRAND_NAME = 'Triplanio';

// Brand logo (SVG), served from our own public/ assets. Used in <img src>
// across the app (header, error page, public trip view, favicon).
export const BRAND_LOGO_URL = '/triplanio-logo.svg';