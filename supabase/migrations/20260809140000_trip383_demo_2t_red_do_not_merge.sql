-- ⚠️ ФИКТИВНАЯ МИГРАЦИЯ — ДЕМОНСТРАЦИЯ КРАСНОТЫ ГАРДА 2t (TRIP-383). НЕ МЁРЖИТЬ.
-- Меняет схему (добавляет колонку) и НАМЕРЕННО не сопровождается регенерацией
-- supabase/functions/_shared/database.types.ts — ровно тот случай, ради которого
-- заведён гард: «миграция поменяла схему, а типы не перегенерили».
-- Ветка существует только ради красного прогона джобы db-types и удаляется после.
alter table public.trips add column if not exists demo_2t_red_column text;
